# SpectraLab — Справочник по интерфейсу

## 1. Введение

**SpectraLab** — это веб-редактор и просмотрщик графики ZX Spectrum. Поддерживает множество графических форматов ZX Spectrum для просмотра, редактирования, конвертации и импорта внешних изображений с продвинутыми алгоритмами дизеринга.

- **Версия:** 1.59.3
- **Лицензия:** MIT
- **Браузер:** любой современный (Chrome, Firefox, Edge, Safari)
- **Установка не требуется** — работает полностью в браузере

![Главное окно приложения](screenshots/main_window.png)

---

## 2. Компоновка интерфейса

SpectraLab использует двухпанельную компоновку:

- **Левая боковая панель** (225px) — все элементы управления, разделённые по вкладкам
- **Основная область холста** — отображение и редактирование изображения

Дополнительные плавающие панели появляются по мере необходимости:

- **Preview** — миниатюрный предпросмотр (в правом нижнем углу)
- **Sprite Editor** — плавающий пиксельный редактор спрайтов
- **Memory Viewer** — просмотр памяти снапшотов как 1-битная графика
- **Floating Palette** — палитра инструментов/цветов в полноэкранном режиме (клавиша Tab)

### Панель вкладок картинок

При открытии нескольких картинок над холстом появляется панель вкладок. Каждая вкладка показывает имя файла. Точка-индикатор отмечает изменённые (несохранённые) картинки. Нажмите на вкладку для переключения; нажмите кнопку закрытия (×) для закрытия картинки.

![Обзор компоновки](screenshots/layout_overview.png)

---

## 3. Файловые операции

### Открытие файла

Нажмите на поле выбора файла вверху боковой панели или **перетащите файл** на холст.

**Поддерживаемые форматы:**
`.scr`, `.rcs`, `.53c`, `.atr`, `.bsc`, `.bsp`, `.bmc4`, `.ifl`, `.mlt`, `.mc`, `.3`, `.img`, `.ga`, `.gap`, `.hlr`, `.stl`, `.nxi`, `.sl2`, `.specscii`, `.sca`, `.btile`, `.wtile`, `.ch$`, `.chr$`, `.ch-`, `.zxp`, `.slp`, `.slw`, `.sna`, `.z80`, `.zip`, `.png`, `.gif`, `.jpg`, `.jpeg`, `.webp`, `.bmp`

### Новая картинка

Нажмите кнопку **New** для открытия диалога создания. Выберите формат:

| Формат | Расширение | Описание |
|--------|------------|----------|
| Screen | .scr | 256×192, битмап + атрибуты |
| ULA+ | .scr | 256×192, палитра 64 цвета |
| IFL | .ifl | 256×192, мультиколор 8×2 |
| MLT | .mlt | 256×192, мультиколор 8×1 |
| Border Screen | .bsc | 384×304, битмап + атрибуты + бордюр |
| Border Screen+ | .bsp | 384×304, битмап + атрибуты + бордюр (с заголовком) |
| BMC4 | .bmc4 | 384×304, мультиколор 8×4 + бордюр |
| RGB3 | .3 | 256×192, триколор RGB (8 цветов) |
| Gigascreen | .img | 256×192, двухкадровое смешение |
| Gigaattr | .ga | 256×192, общий битмап + два атрибутных кадра |
| Gigaattr pattern | .ga | 32×24 ячейки, паттерн-заливка + два атрибутных кадра |
| Gigaattr+ULA+ | .gap | 256×192, общий битмап + два атрибутных кадра + палитра ULA+ |
| Gigaattr+ULA+ pattern | .gap | 32×24 ячейки, паттерн-заливка + два атрибутных кадра + палитра ULA+ |
| Monochrome | .scr | 256×192, только битмап (ink автокорректируется при ink=paper) |
| Monochrome 2/3 | .scr | 256×128, только битмап (ink автокорректируется при ink=paper) |
| Monochrome 1/3 | .scr | 256×64, только битмап (ink автокорректируется при ink=paper) |
| Attributes | .atr | 32×24 цветовые ячейки |
| SPECSCII | .specscii | 32×24 текстовый режим |
| chr$ | .ch$ | Переменный размер, чередующиеся ячейки 8×8 |
| HLR | .hlr | 256×192, High Lores (32×24 gigascreen) |
| STL | .stl | 256×192, Stellar (64×48 мультиколорный gigascreen) |
| ZXP | .zxp | Переменный размер (8-2048px), палитра ULA или ULA+ |

### Сохранение

Нажмите кнопку **Save** или **Ctrl+S**. Файл сохраняется в исходном формате и скачивается браузером.

### ZIP-архивы

При открытии `.zip`-файла появляется модальный диалог со списком файлов в архиве. Нажмите на файл для извлечения и открытия.

![Диалог создания картинки](screenshots/new_picture_dialog.png)

---

## 4. Основные вкладки

Левая панель имеет четыре вкладки: **View**, **Edit**, **Xform** (Transform) и **Sprites**.

---

## 5. Вкладка View

Вкладка View управляет отображением изображения.

### Масштаб (Zoom)

Выберите масштаб из выпадающего списка: **x1, x2, x3, x4, x5, x6, x8, x10, x20**. Также доступны дробные масштабы: **x1/8, x1/4, x1/2**. Можно использовать **Ctrl+колесо мыши** для масштабирования (работает в любой точке панели холста) или клавиши **1-5** для быстрого масштабирования. Ctrl+колесо мыши над левой боковой панелью выполняет стандартное масштабирование браузера.

### Поворот (Rotate)

Выберите угол поворота из выпадающего списка **Rotate** (рядом с Zoom): **0°, 90°, 180°, 270°**. Это чисто визуальный CSS-поворот — все инструменты рисования, рендереры форматов, операции загрузки/сохранения и экспорт PNG не затрагиваются. Координаты мыши автоматически пересчитываются, поэтому карандаш, кисть, заливка и все остальные инструменты редактора работают корректно при любом угле поворота. Панель предпросмотра редактора поворачивается аналогично. Настройка сохраняется между сессиями через localStorage.

### View Settings (раскрываемая секция)

Нажмите заголовок «View Settings» для раскрытия/сворачивания.

#### Паттерн 53c (отображается только для форматов `.53c` / `.atr`)
Появляется в верхней части View Settings при загрузке картинки 53c/atr:
- **Pattern:** выбор паттерна заливки — **Checker**, **Stripes** или **Pattern** (DD/77)
- **Blend colors** — при включении каждое знакоместо отображается как сплошной усреднённый цвет вместо паттерна дизеринга

#### Border (бордюр)
- **Цвет:** Black, Blue, Red, Magenta, Green, Cyan, Yellow, White
- **Размер:** None, Small (16px), Medium (32px)

#### Palette (палитра)
Выберите палитру отображения из выпадающего списка.

Нажмите кнопку 📂 рядом с выпадающим списком, чтобы загрузить пользовательскую палитру из текстового файла (`.txt`, `.pal`, `.csv`). Файл должен содержать 15 или 16 строк, по одному цвету на строку (индексы 0-7 = обычные, 8-15 = яркие). Если указано 15 цветов, чёрный (#000000) автоматически добавляется как индекс 0. Каждая строка может быть в формате `#RRGGBB` (hex), `RRGGBB` (hex без `#`), `#AARRGGBB` или `AARRGGBB` (альфа-канал игнорируется), или три десятичных значения `R G B` (0-255), разделённые пробелами или запятыми. Строки, начинающиеся с `;` или `//`, и пустые строки считаются комментариями. Загруженная палитра отображается как «Custom (loaded)» в выпадающем списке. Она не сохраняется — при перезагрузке страницы восстанавливается палитра по умолчанию.

#### Переключатели
- **Flash** — вкл./выкл. анимации мерцания (также клавиша **F**)
- **Attrs** — показать/скрыть наложение атрибутов. При выключенных атрибутах bitmap отображается как чёрный ink на белом paper (биты 1 = чёрный, биты 0 = белый)
- **Preview** — показать/скрыть панель предпросмотра (также клавиша **~**)

### Grid (раскрываемая секция)

Нажмите заголовок «Grid» для раскрытия/сворачивания.

#### Paper grid (сетка)
- **Основная сетка:** None, 8px, 16px, 24px, 32px
- **Подсетка:** None, 1px, 2px, 4px, 8px, 16px

#### Border grid (сетка бордюра)
- **Сетка бордюра:** None, 8px, 16px, 24px, 32px
- **Подсетка бордюра:** None, 1px, 2px, 4px, 8px, 16px

#### Grid color (цвет сетки)
Выберите цвет сетки из списка: **Default**, **White**, **Gray**, **Black**, **Orange**, **Red**, **Green**. Удобно для улучшения видимости сетки на тёмных или светлых работах.

### Элементы управления для конкретных форматов

#### RGB3
Для формата `.3` (триколор) — выбор режима отображения:
- **Blend dark** — имитация воспринимаемой яркости на реальном ЭЛТ с учётом гашения обратного хода луча
- **Blend** — полная яркость, прямое отображение каналов R/G/B (каждая плоскость управляет своим каналом на полной интенсивности палитры)
- **Emulate flicker** — чередование кадров для имитации реального оборудования

#### Gigascreen
Для форматов `.img` (Gigascreen), `.ga` (Gigaattr), `.gap` (Gigaattr+ULA+), `.hlr` (High Lores) и `.stl` (Stellar) — выбор режима отображения:
- **Blend dark** — имитация воспринимаемой яркости на реальном ЭЛТ с учётом гашения обратного хода луча
- **Blend** — смешанный вид обоих кадров на полной яркости
- **Emulate flicker** — чередование кадров для имитации реального оборудования

#### Режим редактирования Gigascreen
При загрузке формата семейства Gigascreen в боковой панели редактора появляется переключатель режима:
- **Gigascreen** (по умолчанию) — совместное редактирование с виртуальной палитрой 136 цветов. Рисование затрагивает оба кадра одновременно
- **Screen 1** — на основном холсте отображается только кадр 1 со стандартной 16-цветной палитрой ZX. Рисование затрагивает только кадр 1
- **Screen 2** — на основном холсте отображается только кадр 2 со стандартной 16-цветной палитрой ZX. Рисование затрагивает только кадр 2

В режимах Screen 1/Screen 2 плавающий предпросмотр всегда показывает смешанный результат Gigascreen, позволяя видеть влияние покадровых правок на итоговую картинку. Все инструменты (карандаш, линия, заливка, пипетка, копирование/вставка, очистка, отмена/повтор) работают в раздельном режиме. Режим сбрасывается на Gigascreen при смене файла или формата.

Горячие клавиши: **Alt+1** = Screen 1, **Alt+2** = Screen 2, **Alt+3** = Gigascreen (совмещённый режим).

#### Font (шрифт)
Для формата SPECSCII: нажмите **Browse** для загрузки файла шрифта (`.bin`, `.SpecCHR`). По умолчанию используется ROM-шрифт ZX Spectrum.

### Reference Image — справочное изображение (раскрываемая секция)

Загрузка внешнего изображения в качестве образца для рисования за холстом.

- **Load** — выбрать файл изображения
- **Clear** — удалить справочное изображение
- **Show** — переключить видимость
- **Opacity** — прозрачность (5%–80%)
- **X / Y** — смещение позиции (может быть отрицательным)
- **W / H** — пользовательские размеры (пусто = автоподгонка)

Справочные изображения сохраняются в файлах рабочего пространства.

### File Info — информация о файле (раскрываемая секция)

Отображает информацию о загруженном файле:
- **Name** — имя файла
- **Size** — размер файла в байтах
- **Format** — определённый формат
- **Dimensions** — размеры в пикселях
- **Colors used** — количество различных цветов в картинке. Для атрибутных форматов (SCR, IFL, MLT, BMC4, GMX и др.): считает уникальные значения байтов атрибутов (каждая комбинация ink+paper+bright+flash). Для NXI/SL2 и LoRes: считает уникальные индексы палитры. Обновляется в реальном времени при редактировании.
- **Hidden cells** — количество ячеек, где ink равен paper, но bitmap не весь 0x00 и не весь 0xFF, что указывает на невидимые пиксельные данные. Показывается для форматов с редактируемым bitmap. Не показывается для атрибутных форматов с фиксированным bitmap (GMX 160, HLR, 53c, STL). Обновляется в реальном времени при редактировании.
- **Frames** — количество кадров (для SCA-анимаций)
- **Type** — тип данных (для SCA)
- **Delay** — задержка кадра (для SCA)

### Справка

Внизу вкладки View нажмите кнопку **?** для открытия диалога справки с горячими клавишами и информацией о форматах.

![Элементы вкладки View](screenshots/view_tab.png)

---

## 6. Вкладка Edit — Инструменты рисования

Если картинка не загружена, вкладка Edit показывает: «Load a picture or click this tab again to create a new one.»

### Инструменты

| Инструмент | Клавиша | Описание |
|------------|---------|----------|
| Pixel | P | Свободное рисование пикселей |
| Line | L | Рисование прямых линий |
| Rectangle | R | Рисование прямоугольников |
| Circle/Ellipse | O | Рисование окружностей и эллипсов |
| Airbrush | G | Распыление случайных пикселей в радиусе |
| Gradient | D | Рисование дизеринговых градиентов |
| Flood Fill | I | Заливка связанных пикселей одного цвета |
| Fill Cell | C | Заливка знакоместа 8×8 |
| Eraser | E | Стирание пикселей (делает прозрачными на не-фоновых слоях) |
| Text | T | Размещение текста на холсте |
| Recolor | A | Изменение только атрибутов, сохранение битмапа (горячая клавиша) |
| Color Picker | K | Захват цветов с холста (левая=ink, правая=paper) |
| Cell Invert | J | Обмен ink↔paper + инверсия битмапа (цвета не меняются, полярность переключается). Левая кнопка: одна ячейка (перетаскивание для рисования). Правая кнопка: инвертировать все ячейки с таким же атрибутом |
| Dither Brush | W | Передизеринг ячеек другим алгоритмом. Настройки: метод, круглая кисть диаметром (3–16 пикс.), интенсивность. Shift+W — передизеринг текущего выделения |
| Select | S | Выделение прямоугольной области (во вкладке Xform) |

### Модификаторы рисования

| Действие | Эффект |
|----------|--------|
| Левый клик | Рисование цветом ink |
| Правый клик | Рисование цветом paper |
| Alt+клик | Пипетка — захват ink+paper из ячейки |

### Модификаторы фигур (Rectangle, Circle/Ellipse)

| Модификатор | Эффект |
|-------------|--------|
| Ctrl | Ограничение до квадрата/круга (пропорция 1:1) |
| Alt | Рисование от центра вместо угла |
| Ctrl+Alt | Квадрат/круг от центра |

![Инструменты редактора](screenshots/edit_tools.png)

---

## 7. Вкладка Edit — Цвет и палитра

### Стандартная палитра

Палитра показывает 8 цветов ZX Spectrum (0-7). Нажмите для выбора ink (помечается **I**), правый клик для выбора paper (помечается **P**).

Под палитрой:
- **Bright (B)** — переключение атрибута яркости
- **Flash (F)** — переключение атрибута мерцания
- Клавиша **X** — поменять ink и paper местами

### Палитра ULA+

Отображается при редактировании картинок формата ULA+. 64 цвета в 4 CLUT (Color Look-Up Table), по 16 цветов в каждом.

- **Grid** режим — все 64 цвета в сетке 8×8
- **Classic** режим — одна CLUT с кнопками выбора (0, 1, 2, 3)
  - CLUT 0: FLASH=0 BRIGHT=0
  - CLUT 1: FLASH=0 BRIGHT=1
  - CLUT 2: FLASH=1 BRIGHT=0
  - CLUT 3: FLASH=1 BRIGHT=1
- **Переключатель** — смена между Grid и Classic режимами
- **Сохранение палитры** — экспорт 64-байтного `.pal`-файла (кодировка GRB332)
- **Загрузка палитры** — импорт `.pal`-файла
- **Ctrl+клик** на цвете — открытие редактора цвета (R/G/B слайдеры, формат GRB332: 3 бита зелёного, 3 бита красного, 2 бита синего)
- **Shift+клик** на цвете — пометить как источник для копирования (анимированная рамка), затем:
  - **Клик** на другом цвете — скопировать GRB-значение источника в цель
  - **Shift+клик** на другом цвете — поменять оба GRB-значения местами
  - **Клик на том же цвете** или **Escape** — отмена

Ink и paper должны быть из одного CLUT.

### Режим ULANext

Отображается при просмотре/редактировании картинок формата ULANext (расширенная палитра ZX Spectrum Next). ULANext использует настраиваемую маску ink для разделения каждого байта атрибутов между индексами ink и paper, позволяя использовать до 256 цветов ink или 256 цветов paper.

- **Автоопределение** по размеру файла (6945–7426 байт) и валидации маски ink
- **Маска ink** — определяет разделение ink/paper (8 допустимых значений: $01–$FF)
- **Палитра** — записи 8-бит (1-байтный RRRGGGBB) или 9-бит (2-байтный RGB333), определяется автоматически
- **Flash отключён** — биты 6-7 атрибутов используются для индексации палитры
- **Информация о формате** — показывает значение маски, количество цветов ink/paper и разрядность палитры
- Цвета отображаются автоматически; стандартные инструменты рисования работают с битмапом и атрибутами

### Палитра Next (NXI / SL2 / LoRes)

Отображается при редактировании форматов ZX Next с индексированными цветами (NXI, SL2, LoRes, LoRes Radastan). 256 цветов (или 16 для 4bpp-режимов) в сетке из 16 столбцов.

- **Левый клик** — выбор ink
- **Правый клик** — выбор paper
- **Ctrl+клик** на цвете — открытие редактора цвета (R/G/B слайдеры, формат RGB333: 3 бита красного, 3 бита зелёного, 3 бита синего)
- **Shift+клик** на цвете — пометить как источник для копирования (анимированная рамка), затем:
  - **Клик** на другом цвете — скопировать RGB-значение источника в цель
  - **Shift+клик** на другом цвете — поменять оба RGB-значения местами и перенаправить пиксели
  - **Клик на том же цвете** или **Escape** — отмена
- **Сортировка** — None, Luminance, Hue, RGB; опция Reverse
- **Sort by usage** — физически переупорядочивает записи палитры: используемые цвета перемещаются в начало (по убыванию частоты использования пикселей), неиспользуемые — в конец. Индексы пикселей перенаправляются, изображение остаётся неизменным. Отменяется через Ctrl+Z.
- **Сохранение палитры** — экспорт файла палитры
- **Загрузка палитры** — импорт файла палитры

**Палитра и сохранение:** Файлы NXI содержат палитру в заголовке файла — изменения палитры всегда сохраняются. Файлы SL2 с нестандартной палитрой автоматически сохраняют палитру после пиксельных данных (файл увеличивается: 49664, 82432 или 81952 байт); файлы со стандартной палитрой RGB332 сохраняются как только пиксельные данные. Форматы LoRes (SLR) и Radastan (RAD без встроенной палитры) не хранят палитру в файле. При редактировании палитры в этих форматах при сохранении появится предупреждение, что пользовательские цвета будут потеряны при повторной загрузке (будет использована палитра по умолчанию). Файлы Radastan, загруженные со встроенной палитрой (6160 байт GRB332 или 6176 байт RGB333), сохраняют палитру при записи.

### Палитра Gigascreen

Отображается для форматов Gigascreen, Gigaattr, Gigaattr+ULA+, HLR и STL в режиме **Gigascreen**. Сетка из 16 столбцов показывает все виртуальные цвета, создаваемые чередованием двух кадров.

- Левый клик = выбор ink
- Правый клик = выбор paper
- Секция **Cell Colors** показывает 4 доступных цвета для текущей ячейки
  - **L** = цвет левой кнопки мыши
  - **R** = цвет правой кнопки мыши

**Вкладки страниц CLUT** (только для GAP с двойной палитрой): при редактировании файла GAP с двойной палитрой в режиме Gigascreen сетки ink и paper (по 1024 записи) разделяются на 4 страницы по 256 записей. Кнопки **0–3** переключают страницы CLUT (0 = bright=0/flash=0, 1 = bright=1/flash=0, 2 = bright=0/flash=1, 3 = bright=1/flash=1). Каждая страница показывает 8 ink-цветов кадра 1 из одного CLUT × все 32 цвета кадра 2. Стандартный gigascreen (136 записей) отображается без пагинации.

**Полоса цветов paper** (только для GAP с двойной палитрой): отдельная сетка «PAPER COLORS» отображается под сеткой ink-палитры, показывая все перекрёстные смешанные цвета paper из обеих палитр. Клик по образцу paper устанавливает цвет paper. Выбор ink и paper независимы — сетка ink управляет ink, полоса paper управляет paper. Превью Ink/Paper вверху показывают актуальные смешанные цвета для каждого.

При переключении в режим **Screen 1** или **Screen 2** вместо палитры Gigascreen отображается стандартная 16-цветная палитра ZX Spectrum с элементами управления ink, paper, bright и flash для прямого покадрового редактирования атрибутов.

### Палитра 53c/127c

Отображается при редактировании формата 53c/atr. Сетка уникальных цветовых образцов, представляющих все комбинации ink/paper через выбранный паттерн.

- Нажмите на образец для выбора цветовой комбинации
- **Сортировка:** Hue (по умолчанию), RGB или Color (по байту атрибута)
- **Reverse** — переворачивает порядок сортировки
- Адаптируется к текущему паттерну: Checker (~53 цвета), DD77 (~127 цветов)
- Выбранный цвет сохраняется при смене сортировки или паттерна

### Палитра RGB3

Отображается для формата RGB3 (.3). 8-цветная палитра с выбором левым/правым кликом (без отдельных ink/paper). Цвета палитры зависят от режима отображения: Blend — полная яркость RGB, Blend dark — реалистичные воспринимаемые цвета (1/3 яркости на канал × CRT-фактор затемнения), Flicker — чередование кадров.

![Палитра](screenshots/palette_controls.png)

---

## 8. Вкладка Edit — Кисти

### Форма кисти

Шесть форм кисти:

| Форма | Иконка | Описание |
|-------|--------|----------|
| Square | ■ | Квадратная кисть |
| Round | ● | Круглая кисть |
| HLine | — | Горизонтальная линия |
| VLine | \| | Вертикальная линия |
| Stroke | / | Прямой диагональный штрих |
| BStroke | \\ | Обратный диагональный штрих |

### Размер кисти

Выберите размер от 1 до 16 пикселей в выпадающем списке или используйте клавиши **[ ]** для уменьшения/увеличения.

### Предпросмотр кисти

Нажмите **` (обратный апостроф)** для переключения режима предпросмотра кисти — полупрозрачная кисть отображается у курсора.

### Custom Brushes — пользовательские кисти (раскрываемая секция)

12 слотов, расположенных в два ряда по 6. Каждый слот — холст 64×64 пикселя.

| Действие | Эффект |
|----------|--------|
| Клик на слоте | Выбрать кисть (пустой слот начинает захват) |
| Shift+клик на слоте | Захватить кисть с экрана |
| Ctrl+клик на слоте | Очистить слот |

Кнопки **↻ ↔ ↕** во вкладке Xform трансформируют буфер обмена при вставке, в остальных случаях — выбранную пользовательскую кисть.

#### Вкладки кистей

Пользовательские кисти поддерживают вкладки:
- **Custom brushes** — 12 свободных слотов
- **Tileset** — загруженные из `.768`/`.bin` файлов шрифтов/тайлсетов

Используйте кнопки **Save** и **Load** для сохранения/загрузки наборов кистей (формат `.slb`) или тайлсетов (`.768`/`.bin`).

### Barcodes — штрихкоды (паттерны бордюра)

Доступно для форматов BSC/BSP/BMC4. 8 слотов для полосовых паттернов бордюра.

| Действие | Эффект |
|----------|--------|
| Shift+клик на слоте | Начать захват, затем кликнуть на бордюре |
| Клик на слоте | Выбрать/отменить выбор |
| Ctrl+клик на слоте | Очистить слот |

Ширина определяется автоматически (8/16/24px). Выбранные штрихкоды ставятся кликом/перетаскиванием на бордюре.

Сохранение/загрузка наборов штрихкодов кнопками **Save/Load** (формат `.slbc`).

![Пользовательские кисти](screenshots/custom_brushes.png)

---

## 9. Вкладка Edit — Режимы рисования

Выпадающий список **Mode** управляет способом нанесения пикселей:

| Режим | Описание |
|-------|----------|
| Set | Рисование ink или paper в зависимости от кнопки мыши |
| Invert | Переключение пикселей (ink ↔ paper) |
| Replace | Перезапись всех пикселей под кистью |
| Recolor | Изменение только атрибутов, сохранение битмапа |
| Retouch | Изменение только битмапа, сохранение атрибутов |
| Masked | Тайловая маска из пользовательской кисти (фиксированное начало) |
| Mask+ | Тайловая маска из пользовательской кисти (начало от штриха) |

**Режимы Masked:** выберите пользовательскую кисть как паттерн/трафарет, затем выберите любую форму/размер кисти для рисования. В режиме Masked паттерн привязан к началу координат холста; в режиме Mask+ начало паттерна следует за началом штриха.

### Режим привязки (Snap)

Выпадающий список **Snap** управляет привязкой курсора:

| Режим | Описание |
|-------|----------|
| Grid | Привязка к размеру сетки (из вкладки View) |
| Grid× | Привязка к сетке, выравнивание по углу |
| Sub | Привязка к размеру подсетки |
| Sub× | Привязка к подсетке, выравнивание по углу |
| Zero | Привязка к сетке размером с буфер обмена от начала координат |
| Brush | Привязка к сетке размером с буфер обмена от первой вставки |
| Off | Пиксельная точность |

---

## 10. Вкладка Edit — Инструмент Text

При выборе инструмента Text (T) появляется секция:

- **Поле ввода текста** — введите текст для размещения
- **Выбор шрифта** — выбор из загруженных шрифтов (по умолчанию: ROM/Spectrum)
- **Размер шрифта** — 8, 12, 16, 20, 24 или 32 пикселя
- Кнопка **.768** — загрузить 8×8 битмапный шрифт ZX Spectrum (`.768`, `.ch8`, `.bin`)
- Кнопка **TTF** — загрузить шрифт TrueType/OpenType (`.ttf`, `.otf`, `.woff`, `.woff2`)
- Кликните на холсте для размещения текста

![Инструмент Text](screenshots/text_tool.png)

---

## 11. Вкладка Edit — Инструмент Airbrush

При выборе инструмента Airbrush (G) появляются дополнительные элементы:

- **Radius** — радиус распыления: 4, 8, 12, 16, 24 или 32 пикселя
- **Density** — плотность: 0.03–1.0
- **Falloff** — распределение: Uniform, Soft, Medium, Hard, Very Hard (смещение к центру)

Удерживайте кнопку мыши для непрерывного распыления. Работает с режимом Masked для распыления через трафарет.

---

## 12. Вкладка Edit — Инструмент Gradient

При выборе инструмента Gradient (D) появляются дополнительные элементы:

- **Type** — Linear, Radial, Diamond, Conical, Square, Spiral
- **Dither** — Bayer (упорядоченный) или Noise (голубой шум)
- **Reverse** — инвертировать направление ink/paper

Перетащите от начальной к конечной точке для создания градиента.

---

## 13. Вкладка Edit — Слои (Layers)

Секция Layers (раскрываемая) позволяет многослойное редактирование:

- **Add** (+) — добавить новый слой (при первом использовании создаёт фоновый + новый слой)
- **Remove** (−) — удалить выбранный слой
- **Move Up** (↑) / **Move Down** (↓) — изменить порядок слоёв
- **Flatten** (⊟) — объединить все слои в один

Сохранение/загрузка проектов со слоями кнопками **Save/Load** (формат `.slp`).

---

## 14. Вкладка Edit — Редактор SPECSCII

Для картинок формата SPECSCII появляется палитра символов (сворачиваемая):

- **Сетка символов** — ROM-шрифт (0x20-0x7F) и блочная графика (0x80-0x8F)
- Клик на тайле для выбора символа для рисования
- **Сортировка по весу** (кнопка Sort) — переключает между порядком Code (последовательный) и Weight (сортировка по количеству пикселей, от лёгких к тяжёлым; по умолчанию). Блочная графика отображается в виде симметричной сетки 5×3 в режиме Weight. Настройка сохраняется в localStorage
- **Предпросмотр символа** — увеличенное отображение выбранного символа с его кодом
- Правый клик на холсте для захвата символа + атрибутов с экрана

Доступные инструменты в режиме SPECSCII: Pixel, Line, Rectangle, Circle, Flood Fill, Eraser, Text.

Режимы рисования: Set (размещение символа), Invert (обмен ink/paper), Recolor (только атрибуты). Eraser очищает ячейку и удаляет её из текущего слоя.

SPECSCII поддерживает OVER-слои (XOR-композитинг), загружаемые из управляющих кодов потока. Экспорт SPECSCII в `.scr` через вкладку Transform.

![Редактор SPECSCII](screenshots/specscii_editor.png)

---

## 15. Вкладка Transform (Xform)

### Workspace (рабочее пространство)

- **Save Workspace** — сохранить все открытые картинки и спрайты в `.slw`-файл
- **Load Workspace** — загрузить файл рабочего пространства (`.slw`)

Кнопки Workspace всегда доступны, даже без загруженной картинки. Остальные инструменты трансформации требуют загрузки или создания картинки.

### Сохранить все картинки (Save all pictures)

Группа видна только когда **открыто две или более картинок**. Все четыре варианта используют базовое имя файла `spectralab_pictures` (с расширением `.zip` / `.gif` / `.sca`).

- **ZIP (originals)** — упаковывает все открытые картинки в их родном бинарном формате в один `.zip`. Каждый файл сохраняет своё исходное расширение (например, `.scr`, `.bsc`, `.ifl`, `.img`); дублирующиеся имена различаются суффиксами ` (2)`, ` (3)`, …
- **ZIP (PNG / GIF)** — упаковывает все картинки, отрендеренные с текущими настройками вида (масштаб, бордюр, палитра, фильтры дисплея), в виде PNG. Картинки с мерцающими атрибутами сохраняются как двухкадровые анимированные GIF на стандартной частоте FLASH.
- **Animated GIF** — собирает все картинки в один анимированный GIF, 500 мс на кадр. Картинки с мерцанием дают два кадра (обычная фаза + инвертированная). Требуется, чтобы все картинки рендерились в один и тот же размер холста — при смешении форматов с разными размерами экспорт прерывается с ошибкой.
- **SCA** — собирает все картинки в один SCA-файл анимации, 500 мс на кадр. Требуется, чтобы все картинки были в обычном формате SCR (256×192, 6912 байт); цвет бордюра берётся из текущей настройки. При наличии любой картинки в другом формате экспорт прерывается со списком несовместимых файлов.

Активная картинка сохраняется — операция кратковременно перебирает все открытые картинки для рендера и затем возвращается на исходную вкладку.

### History (история)

- **Undo** (Ctrl+Z) — отмена действия (до 32 уровней)
- **Redo** (Ctrl+Y) — повтор отменённого действия
- **Clear** — очистка всего экрана

### Конвертация формата

Выпадающий список **Convert to...** для конвертации текущей картинки в другой формат.

Доступные конвертации:
- **Без потерь**: SCR ↔ ATTR, SCR ↔ BSC ↔ BSP, SCR ↔ ULA+, GA ↔ GAP (добавить/убрать палитру ULA+), NXI ↔ SL2 (все режимы: 256×192, 320×256, 640×256). При конвертации NXI → SL2 с нестандартной палитрой появляется диалог: сохранить палитру (встроить в SL2), квантовать к стандартной RGB332 (с потерями) или убрать палитру (оставить индексы без изменений)
- **Переключение палитры GAP**: общая палитра → двойная палитра (дублирует палитру 1 в палитру 2, позволяя независимые палитры для каждого кадра); двойная палитра → общая палитра (отбрасывает палитру 2). Формат не меняется — меняется только количество палитр. Также доступна конвертация Gigascreen ↔ GAP (убирает/добавляет палитру + независимые битмапы)
- **53c → GA/GAP pattern**: конвертация ATTR (.53c) в паттерн-режим GA или GAP. Три варианта: "GA (Gigaattr pattern)", "GAP (Gigaattr+ULA+ pattern, shared)", "GAP (Gigaattr+ULA+ dual pattern)". Открывается комбинированный диалог: сетка паттернов-заливки, выбор цвета бордюра 1 и 2 (8 цветов ZX). Для вариантов GAP палитра ULA+ генерируется автоматически. Результат — компактный файл (1546/1610/1674 байт)
- **С потерями (рендер + квантизация)**: SCR/ULA+ → NXI 320×256/640×256, NXI/SL2 между режимами (256↔320↔640), NXI/SL2 → SCR
- **Символьное сопоставление**: SCR → SPECSCII — сопоставляет битовый паттерн каждой ячейки 8×8 с наиболее подходящим символом ROM-шрифта или блочной графикой, сохраняя яркость и мерцание; проверяет как прямое, так и инвертированное (обмен ink/paper) сопоставление; скрытые пиксели (ink == paper) сопоставляются с наиболее подходящим глифом, чтобы битовый паттерн сохранялся для редактирования

### Экспорт

- Выпадающий список формата экспорта (доступные варианты зависят от загруженного формата)
- Флажок **Embed** — встраивание данных как DB-строк в ASM-файл (выключен = использовать INCBIN-ссылки)
- Кнопка **Export** — экспорт в выбранный формат
- Кнопка **PNG/GIF** — экспорт текущего экрана в PNG или анимированный GIF
  - Использует все текущие настройки вкладки View: зум, размер/цвет бордюра, сетка/подсетка, палитра, фильтры
  - Для форматов семейства gigascreen (Gigascreen, Gigaattr, Gigaattr+ULA+, MGH, HLR, STL, BSP-gigascreen, chr$-gigascreen): диалог выбора **Blended** (усреднённые цвета → PNG) или **Flicker** (два чередующихся кадра → анимированный GIF ~50fps)
  - Для картинок с атрибутами flash: диалог выбора **Animated GIF** (двухкадровая анимация мерцания, 320мс на фазу) или **Static PNG** (только нормальная фаза)
  - Для остальных форматов: экспорт напрямую в PNG

### Экспорт в RCS

Для формата **SCR** в выпадающем списке Export доступен вариант `.rcs (RCS reordered)` — экспорт экрана в формате [RCS (Re-ordered Compressed Screen)](https://github.com/einar-saukas/RCS) от Einar Saukas. 6144 байта битмапа переупорядочиваются по схеме S→C→R→L (сектор → столбец → символьная строка → пиксельная линия), группируя пространственно связанные байты вместе для значительно лучшего сжатия упаковщиками типа ZX7. 768 байт атрибутов добавляются без изменений. Выход: 6912-байтный файл `.rcs`.

RCS-файлы также можно открывать напрямую — переупорядочивание автоматически обращается при загрузке, конвертируя обратно в стандартный SCR для просмотра и редактирования.

### Сжатие ZX7 / ZX0

Для формата **SCR** в выпадающем списке Export доступны варианты сжатия [ZX7](https://spectrumcomputing.co.uk/entry/27996/ZX-Spectrum/ZX7) и [ZX0](https://github.com/einar-saukas/ZX0) (формат v2) от Einar Saukas:

- **`.rcs.zx0 (RCS + ZX0)`** — сначала переупорядочивание RCS, затем сжатие ZX0
- **`.rcs.zx7 (RCS + ZX7)`** — сначала переупорядочивание RCS, затем сжатие ZX7
- **`.scr.lgk (LgK compressed)`** — сжатие данных экрана алгоритмом LgK v1.1rs (тайловое XOR-предсказание с кодированием Хаффманом режимов трансформации, RLE атрибутов с оптимизацией палитры; распаковщик 612 байт)
- **`.scr.lc (LC compressed)`** — сжатие данных экрана алгоритмом Laser Compact 5.2.1 (включает заголовок LCMP5; переупорядочивание и обработка сегментов встроены)
- **`.scr.c4 (Chunks 4×4)`** — монохромное сжатие с потерями: делит каждую ячейку 8×8 на четыре квадранта 4×4, кодирует каждый 2-битным индексом в словарь из 4 паттернов. Фиксированный размер: 841 байт (768Б закодированных данных + 64Б таблица подстановок + 8Б словарь + 1Б режим). Очень быстрый Z80-распаковщик с поиском по полубайтовой таблице
- **`.scr.c2 (Chunks 4×2)`** — монохромное сжатие с потерями: делит каждую ячейку 8×8 на восемь полос 4×2, кодирует каждую 2-битным индексом. Фиксированный размер: 1573 байта (1536Б закодированных данных + 32Б таблица подстановок + 4Б словарь + 1Б режим). Лучшее качество чем 4×4, всё ещё быстрый Z80-распаковщик
- **`.scr.lzf (ZXSC / LZF)`** — сжатие данных экрана алгоритмом LZF (стандартный линейный режим, распаковщик 49 байт)
- **`.scr.lzf (ZXSC screen-scan)`** — сжатие с нелинейным посимвольным переупорядочиванием (атрибут + 8 пиксельных строк на ячейку, слева направо, сверху вниз) затем LZF. Даёт визуально приятную распаковку на ZX Spectrum (распаковщик 80 байт)
- **`.scr.rle (RLE compressed)`** — сжатие данных экрана алгоритмом RLE в стиле PackBits (распаковщик 23 байта, быстрая распаковка)
- **`.scr.upk (upkr level 1)`** — сжатие данных экрана алгоритмом upkr (rANS-энтропийное кодирование, настройки Z80, быстрое сжатие)
- **`.scr.upk (upkr level 9)`** — сжатие данных экрана алгоритмом upkr (rANS-энтропийное кодирование, настройки Z80, лучшее сжатие)
- **`.scr.zx0 (ZX0 compressed)`** — сжатие данных экрана алгоритмом ZX0 (прямой режим)
- **`.scr.zx7 (ZX7 compressed)`** — сжатие данных экрана алгоритмом ZX7 (прямой режим)
- **`Compare compressions...`** — открывает диалог сравнения вариантов сжатия:
  - Plain SCR (6912 байт, несжатый базовый вариант)
  - ZX7 / ZX7 backwards (обратный)
  - RCS + ZX7 / RCS + ZX7 backwards (обратный)
  - ZX0 / ZX0 backwards (обратный)
  - RCS + ZX0 / RCS + ZX0 backwards (обратный)
  - LgK / LgK (opt)
  - LC (Laser Compact 5.2.1)
  - upkr level 1 / upkr level 9
  - RLE
  - ZXSC (линейный LZF) / ZXSC screen (посимвольный LZF)
  - Chunks 4×4 / Chunks 4×2 (с потерями)

  Диалог открывается с пустой таблицей (все размеры отображаются как «—»). Нажмите кнопку **Compare** для запуска сжатия — результаты появляются по одному, лучший вариант подсвечивается и выбирается автоматически. Диалог остаётся открытым после сохранения, что позволяет экспортировать несколько форматов без повторного запуска сжатия.

  **⚙ Настройки форматов** (иконка шестерёнки в заголовке) — открывает панель настроек для включения/отключения семейств форматов (ZX7, ZX0, варианты RCS, LgK, LC, upkr, RLE, ZXSC, Chunks). Отключённые форматы исключаются из таблицы сравнения. Для upkr можно выбрать вариант распаковщика: Compact (130 байт код + 320 байт массив вероятностей = 450 байт) или Fast (155 байт код + 320 байт массив вероятностей = 475 байт, развёрнутый цикл умножения). Все настройки сохраняются в localStorage и сохраняются между сессиями.

  Выпадающий список **Data** — выбор сжимаемой области данных:
  - **Full SCR** (по умолчанию) — полные 6912 байт (битмап + атрибуты)
  - **Bitmap only** — 6144 байта (только битмап, без атрибутов)

  Выпадающий список **Segment** (доступен только при Data = «Bitmap only») — выбор сегмента битмапа:
  - **Whole** — все 6144 байт битмапа
  - **Third 1 / 2 / 3** — отдельные трети битмапа по 2048 байт
  - **Thirds 1+2 / 2+3** — две последовательные трети (4096 байт)

  Варианты RCS отображаются только при Segment = «Whole» (RCS переупорядочивает весь 6144-байтный битмап; разрезание после переупорядочивания бессмысленно). Для отдельных сегментов отображаются только plain + ZX7/ZX0 прямой/обратный.

  **Clean hidden cells** — при включённом чекбоксе скрытые ячейки (ink === paper с нетривиальным битмапом) очищаются на временной рабочей копии перед сжатием, используя плотность битмапа соседних ячеек для выбора значения заливки. Исходное изображение не изменяется.

  **Optimize attributes** — при включённом чекбоксе ink/paper меняются местами, а битмап инвертируется в ячейках, где установленных битов больше, чем сброшенных (режим «minimize ink bits»), на той же временной копии. Обе оптимизации можно комбинировать; переключение любого чекбокса сбрасывает таблицу в «—» (устаревшие результаты).

  Изменение любой опции (Data, Segment, чекбоксы) сбрасывает таблицу — нажмите Compare для повторного запуска.

  Столбец **Depacker** — показывает размер Z80-распаковщика в байтах для каждого метода, включая код и необходимый буфер: ZX7 прямой/обратный = 69 байт, ZX0 прямой = 68, ZX0 обратный = 69. Варианты RCS используют [«умные» интегрированные декодеры](https://github.com/einar-saukas/RCS), распаковывающие и декодирующие напрямую в экран без временного буфера: RCS+ZX7 = 110, RCS+ZX7 обратный = 110, RCS+ZX0 = 112, RCS+ZX0 обратный = 113. LgK = 612 байт (самомодифицирующийся распаковщик, распаковывает напрямую в экран). LC = 209 байт (распаковывает напрямую в экран, без дополнительного буфера). upkr = 450 байт (130 код + 320-байтный массив вероятностей). RLE = 23 байта. ZXSC = 49 байт (стандартный) / 80 байт (screen-scan). Chunks 4×4 = 139 байт (75 код + 64-байтная таблица подстановок), Chunks 4×2 = 97 байт (65 код + 32-байтная таблица подстановок). У несжатого варианта распаковщик отсутствует.

  Столбец **Total** — показывает реальную экономию: сэкономленные байты минус накладные расходы на распаковщик (saved − depacker). Положительное значение означает, что сжатие выгодно даже с учётом распаковщика. Отрицательное значение (выделено красным) означает, что сжатые данные плюс распаковщик превышают исходный размер. Для вариантов Chunks с потерями значение «saved» отражает только уменьшение размера — качество битмапа приблизительное. Для upkr можно выбрать вариант распаковщика: Compact (130 байт код + 320 байт массив вероятностей = 450 байт) или Fast (155 байт код + 320 байт массив вероятностей = 475 байт, развёрнутый цикл умножения). Все настройки сохраняются в localStorage и сохраняются между сессиями.

  Выпадающий список **Data** — выбор сжимаемой области данных:
  - **Full SCR** (по умолчанию) — полные 6912 байт (битмап + атрибуты)
  - **Bitmap only** — 6144 байта (только битмап, без атрибутов)

  Выпадающий список **Segment** (доступен только при Data = «Bitmap only») — выбор сегмента битмапа:
  - **Whole** — все 6144 байт битмапа
  - **Third 1 / 2 / 3** — отдельные трети битмапа по 2048 байт
  - **Thirds 1+2 / 2+3** — две последовательные трети (4096 байт)

  Варианты RCS отображаются только при Segment = «Whole» (RCS переупорядочивает весь 6144-байтный битмап; разрезание после переупорядочивания бессмысленно). Для отдельных сегментов отображаются только plain + ZX7/ZX0 прямой/обратный.

  **Clean hidden cells** — при включённом чекбоксе скрытые ячейки (ink === paper с нетривиальным битмапом) очищаются на временной рабочей копии перед сжатием, используя плотность битмапа соседних ячеек для выбора значения заливки. Исходное изображение не изменяется.

  **Optimize attributes** — при включённом чекбоксе ink/paper меняются местами, а битмап инвертируется в ячейках, где установленных битов больше, чем сброшенных (режим «minimize ink bits»), на той же временной копии. Обе оптимизации можно комбинировать; переключение любого чекбокса сбрасывает таблицу в «—» (устаревшие результаты).

  Изменение любой опции (Data, Segment, чекбоксы) сбрасывает таблицу — нажмите Compare для повторного запуска.

  Столбец **Depacker** — показывает размер Z80-распаковщика в байтах для каждого метода, включая код и необходимый буфер: ZX7 прямой/обратный = 69 байт, ZX0 прямой = 68, ZX0 обратный = 69. Варианты RCS используют [«умные» интегрированные декодеры](https://github.com/einar-saukas/RCS), распаковывающие и декодирующие напрямую в экран без временного буфера: RCS+ZX7 = 110, RCS+ZX7 обратный = 110, RCS+ZX0 = 112, RCS+ZX0 обратный = 113. LgK = 612 байт (самомодифицирующийся распаковщик, распаковывает напрямую в экран). LC = 209 байт (распаковывает напрямую в экран, без дополнительного буфера). upkr = 450 байт (130 код + 320-байтный массив вероятностей). RLE = 23 байта. ZXSC = 49 байт (стандартный) / 80 байт (screen-scan). Chunks 4×4 = 139 байт (75 код + 64-байтная таблица подстановок), Chunks 4×2 = 97 байт (65 код + 32-байтная таблица подстановок). У несжатого варианта распаковщик отсутствует.

  Столбец **Total** — показывает реальную экономию: сэкономленные байты минус накладные расходы на распаковщик (saved − depacker). Положительное значение означает, что сжатие выгодно даже с учётом распаковщика. Отрицательное значение (выделено красным) означает, что сжатые данные плюс распаковщик превышают исходный размер.

**Create ASM** — при включённом чекбоксе при сохранении также генерируется `.asm`-файл для sjasmplus. Генерация ASM доступна только для режима Full SCR + Whole. ASM-файл содержит полный рабочий пример: декомпрессия данных напрямую в область экрана `$4000`, встроенный декомпрессор ZX7, ZX0, LgK, LC или upkr (прямой или обратный, где применимо) и процедура переупорядочивания RCS→SCR при необходимости, используются `device zxspectrum48` и `savesna` для создания `.sna`-снимка. Вариант LgK использует самомодифицирующийся распаковщик Lethargeek, который распаковывает напрямую в экран (HL=сжатые данные). Вариант LC использует распаковщик Laser Compact 5.2 от Hrumer, который распаковывает данные с заголовком LCMP5 напрямую в экран. Вариант upkr использует Z80-распаковщик Петера Хельцмановского (IX=сжатые данные, DE'=адрес назначения через EXX).

Прямое сжатие использует расширения `.zx7`/`.zx0`, обратное — `.zx7b`/`.zx0b`. Сжатие LgK использует расширение `.lgk`. Сжатие LC использует расширение `.lc`. Сжатие upkr использует расширение `.upk`. Все варианты можно открывать напрямую — декомпрессия (и обратное переупорядочивание RCS при необходимости) выполняется автоматически при загрузке.

### Экспорт формата в ASM

Выпадающий список Export генерирует самодостаточные ASM-файлы, совместимые с sjasmplus, которые отображают загруженную картинку на реальном оборудовании. Каждый экспорт создаёт полную программу-просмотрщик — достаточно ассемблировать и запустить.

Доступные форматы экспорта:

| Формат | Вариант экспорта | Платформа | Выход |
|--------|-----------------|-----------|-------|
| BSC | ASM (Pentagon border) | Pentagon 128K | .sna — потактовые эффекты бордюра |
| Gigascreen / MGH | ASM (Pentagon dual-screen) | Pentagon 128K | .sna — чередование экранных банков |
| Gigaattr | ASM (48K attr flicker) | ZX Spectrum 48K | .sna — LDIR битмапа один раз, чередование атрибутов каждый кадр |
| Gigaattr+ULA+ | ASM (48K ULA+ flicker) | ZX Spectrum 48K + ULA+ | .sna — программирование палитры ULA+ + чередование атрибутов (общая: программирование один раз; двойная: перепрограммирование каждый кадр) |
| RGB3 | ASM (Pentagon RGB flicker) | Pentagon 128K | .sna — мерцание RGB-каналов |
| IFL | ASM (Pentagon 8x2 multicolor) | Pentagon 128K | .sna — мультиколор 8×2 |
| ULA+ | ASM (ULA+ palette) | ZX Spectrum 48K + ULA+ | .sna — программирование палитры из 64 цветов |
| NXI / SL2 | ASM (Next Layer 2 .nex) | ZX Spectrum Next | .nex — Layer 2 с палитрой, все режимы (256×192, 320×256, 640×256) |

- Флажок **Embed** управляет встраиванием пиксельных данных как DB-строк или ссылками INCBIN на исходный файл. Данные палитры всегда встроены (малый размер). Для RGB3 данные всегда встроены.
- Все экспорты предназначены для ассемблера **sjasmplus**. Экспорты для Pentagon создают .sna-снапшоты; экспорт Next Layer 2 создаёт .nex-файлы через SAVENEX.

### Optimize Attributes (оптимизация атрибутов)

Доступно только для формата **SCR**. Автоматически меняет местами ink↔paper и инвертирует биты битмапа в ячейках 8×8, где текущее назначение неоптимально. Отображаемые цвета остаются идентичными — меняется только полярность битмапа и роли ink/paper. Ячейки с ink == paper или flash пропускаются.

Четыре режима:

| Режим | Правило | Применение |
|-------|---------|------------|
| Paper = lighter color | Переворот, если paper темнее ink (по яркости) | Естественный вид — paper всегда светлый фон |
| Paper = majority pixels | Переворот, если более половины пикселей — ink (установленные биты) | Уменьшение плотности ink — больше «белого» в битмапе |
| Combined | Переворот, если paper темнее, или при равной яркости ink в большинстве | Общая очистка — учитывает яркость и плотность |
| Minimize ink bits | Переворот, если установленных битов больше, чем нулевых | Лучшее сжатие — меньше единичных битов = лучший коэффициент RLE/LZ |

Информационная метка показывает количество перевёрнутых ячеек. Операция отменяема (Ctrl+Z).

### Clean Hidden Cells (очистка скрытых ячеек)

Доступно для форматов с битмапом и атрибутами: **SCR**, **BSC**, **IFL**, **MLT**, **BMC4**, **GMX**, **Gigascreen**, **ULA+**. Скрыто для форматов только с атрибутами (GMX 160, HLR, 53c, STL, SPECSCII) и форматов без атрибутов.

«Скрытая ячейка» — это ячейка, где ink равен paper, но битмап неоднороден (не весь 0x00 и не весь 0xFF). Пиксельный рисунок невидим на экране, так как оба цвета одинаковы, но он занимает байты и увеличивает размер при сжатии.

Нажмите **Apply** для очистки всех скрытых ячеек. Для каждой скрытой ячейки инструмент анализирует плотность битмапа четырёх соседних ячеек (вверх, вниз, влево, вправо):
- Если большинство битов соседних ячеек установлены → заполнение 0xFF
- Иначе → заполнение 0x00
- Если соседей нет (угол/край без валидных соседей) → заполнение 0x00

Это сохраняет визуальную непрерывность с окружающими ячейками. Информационная метка показывает количество очищенных ячеек. Операция отменяема (Ctrl+Z). Счётчик «Hidden cells» в File Info обновляется после очистки.

### Clipboard (буфер обмена)

| Кнопка | Клавиша | Описание |
|--------|---------|----------|
| Select | S | Выделение прямоугольной области |
| Cut | Ctrl+X | Вырезание (копирование + стирание) |
| Paste | Ctrl+V | Вставка содержимого буфера |
| Invert | N | Инвертирование (обмен ink ↔ paper) |
| Re-dither | Shift+W | Передизеринг выделения текущим методом кисти |
| Rotate | R | Поворот буфера/кисти на 90° по часовой |
| Flip H | H | Отражение буфера/кисти по горизонтали |
| Flip V | V | Отражение буфера/кисти по вертикали |

- Флажок **Snap to grid** — привязка позиции вставки к сетке
- При вставке кликните для размещения; клавиши R/H/V для трансформации

### Transform Selection

При активном выделении появляются кнопки:
- **↻ Rotate** — поворот выделения на 90° по часовой
- **↔ Mirror H** — зеркальное отражение по горизонтали
- **↕ Mirror V** — зеркальное отражение по вертикали

### Экспорт в ASM

При активном выделении появляется секция «Export Selection to ASM»:

- **Include attributes** — включить данные атрибутов
- **Режим атрибутов:** Attributes after bitmap или Interleaved (byte + attr)
- **Режим строк:** Line-based (полная строка в DEFB) или Block-based (8 байт в DEFB)
- **Направление:** Left to right, Right to left, Zigzag (start L-R), Zigzag (start R-L)
- **Visual comments (█·)** — визуальные комментарии из блочных символов
- **Save ASM file** / **Copy** — сохранить в файл или скопировать в буфер обмена

### Reset to Defaults

Сброс всех сохранённых настроек и перезагрузка приложения.

![Вкладка Transform](screenshots/transform_tab.png)

---

## 16. Вкладка Sprites

**Спрайтсет** — именованная запись в списке, содержащая один или несколько отдельных **спрайтов** (кадров анимации). Каждый спрайтсет имеет фиксированные размеры (W × H) и цветовой режим.

### Список спрайтсетов

Показывает все определённые спрайтсеты. Нажмите для выбора.

- **Клик** — выбрать один спрайтсет
- **Ctrl+Клик** — добавить/убрать спрайтсет из множественного выделения
- **Shift+Клик** — выделить диапазон спрайтсетов от последнего нажатого до текущего
- **Двойной клик** — выбрать и открыть в редакторе спрайтов
- **Правый клик** — открыть контекстное меню с операциями над выбранными спрайтсетами
- **+ Add** — добавить новый спрайтсет
- **Delete** — удалить выбранные спрайтсеты
- **Clear all** — удалить все спрайтсеты (с подтверждением)

### Контекстное меню (правый клик на списке спрайтсетов)

- **Merge selected to animation** — объединить выбранные спрайтсеты (с одинаковыми размерами и режимом) в один спрайтсет с несколькими спрайтами
- **Add frames to…** — скопировать спрайты из выбранных спрайтсетов в указанный целевой спрайтсет
- **Move frames to…** — переместить спрайты в целевой спрайтсет, удалив исходные
- **Split frames to sprites** — разбить многокадровый спрайтсет на отдельные однокадровые (напр. `Name_f1`, `_f2`, …)
- **Delete selected** — удалить все выбранные спрайтсеты

### Свойства спрайтсета

При выборе спрайтсета:

- **Name** — имя спрайтсета (макс. 16 символов)
- **W / H** — размеры в знакоместах (1-8 × 1-8, каждое = 8×8 пикселей); блокируются после рисования (очистите все спрайты для разблокировки)
- **Mode** — Mono, Attributed или Multicolour (8×1 / 8×2 / 8×4 — высота ячейки атрибутов); блокируется после рисования
- **+ Add** — добавить новый пустой спрайтсет
- **Edit** — открыть плавающий редактор спрайтов
- **Delete** — удалить выбранные спрайтсеты
- **Use as Brush** — использовать спрайт как кисть на основном холсте

### Grab from Screen — захват с экрана

Захват спрайтов из загруженной картинки. Установите **W / H** и **Mode** в свойствах спрайтсета перед захватом — захват использует эти значения для размера ячеек и цветового режима.

- Кнопка **Grab** — войти в режим захвата, перетащить прямоугольник на холсте
- Кнопка **Stop** / Escape — выйти из режима захвата
- Каждый захват создаёт **новый спрайтсет** с захваченными спрайтами
- **Режим захвата:**
  - Single sprite — захват одного спрайта
  - Sprite phases — захват как спрайтов анимации
  - Singles grid — захват сетки отдельных спрайтсетов
  - Phases grid — захват сетки наборов спрайтов
- **Настройки сетки:** размер по ячейкам (используются W/H из свойств спрайтсета) или количеству, столбцы/строки, порядок (L→R,T→B или T→B,L→R)

### File (файлы)

- **Save .sls** — сохранить лист спрайтов
- **Load .sls** — загрузить лист спрайтов
- **Export ASM** — экспорт спрайтов как ассемблерный код
- **Export BIN** — экспорт спрайтов как бинарные данные

### Memory Viewer

После загрузки файла снапшота (`.sna`, `.z80`) появляется кнопка **Memory Viewer**. См. раздел 19.

![Редактор спрайтов](screenshots/sprite_editor.png)

---

## 17. Sprite Editor — редактор спрайтов (плавающая панель)

Sprite Editor открывается как плавающая перетаскиваемая панель.

### Инструменты рисования

| Инструмент | Клавиша | Описание |
|------------|---------|----------|
| Draw | D | Рисование пикселей (левая кнопка = ink/установить, правая = paper/стереть) |
| Erase | E | Стирание пикселей (левая кнопка = стереть, правая = установить) |
| Fill | F | Заливка (левая кнопка = заливка ink, правая = заливка paper) |
| Line | L | Линии (левая кнопка = ink, правая = paper) |
| Rectangle | R | Прямоугольники (левая кнопка = ink, правая = paper) |
| Select | S | Выделение области |
| Mask | M | Переключение редактирования маски |

Кнопки мыши работают так же, как на основном холсте: **левая кнопка** рисует ink (устанавливает биты), **правая кнопка** рисует paper (сбрасывает биты).

### Управление цветом

Для спрайтов в режиме Attributed и Multicolour:
- Двухрядная палитра: верхний ряд — 8 обычных цветов, нижний — 8 ярких
- Левый клик по образцу задаёт ink, правый — paper
- Клик по цвету в любом ряду устанавливает bright для этого ряда, перемещая ink и paper в одну яркость
- Цвета соответствуют выбранной **палитре** на вкладке View — при смене палитры отображения обновляются и образцы цветов, и рендер холста спрайта в реальном времени

### Предпросмотр и анимация

- **Preview canvas** — предпросмотр спрайта 1×
- **Onion skin** — показ предыдущего спрайта как полупрозрачное наложение
- **Grid** — показ пиксельной сетки
- **Attributes** — показ цветов атрибутов (включён по умолчанию); при выключении отображает только чёрно-белый bitmap (чёрный = ink, белый = paper)
- **Show mask** — отображение маски спрайта
- **Навигация по спрайтам:** Previous (**<**), счётчик спрайтов, Next (**>**)
- **Панель кадров** — миниатюры спрайтов анимации (16 в строке) с поддержкой множественного выделения
  - Клик — выбрать один спрайт
  - Ctrl+Клик — переключить спрайт в/из множественного выделения
  - Shift+Клик — выделить диапазон от якоря до выбранного спрайта
- **Move Left** (◄) / **Move Right** (►) — переместить выделенные спрайты
- **Add sprite** (+) — добавить спрайт анимации
- **Duplicate sprite** (Dup) — дублировать текущий спрайт
- **Delete sprite** (Del) — удалить текущий или все выделенные спрайты (минимум 1 останется)
- **Play** — анимировать спрайты
- **Speed** — слайдер скорости анимации (1-30)

### Кнопки трансформации

- **FlipH** / **FlipV** — отражение по горизонтали/вертикали
- **RotCW** / **RotCCW** — поворот по/против часовой стрелки
- Флажок **Scroll attr** — при включении стрелки сдвига также прокручивают атрибуты; атрибуты перемещаются, когда накопленный пиксельный сдвиг достигает границы ячейки атрибутов (напр. каждые 2 пикселя для 8×2, каждый 1 пиксель для 8×1); скрыт в режиме Mono
- **← → ↑ ↓** — сдвиг спрайта на 1 пиксель (с опциональной прокруткой атрибутов)
- **Inv** — инвертировать все пиксели
- **Clr** — очистить текущий спрайт

---

## 18. Image Import — импорт изображений

При загрузке файла PNG, JPG, WebP, BMP или GIF открывается диалог Image Import.

Для многокадровых анимированных GIF рядом с кнопкой Import появляется **выпадающий список режимов**:
- **Picture** — импорт текущего просматриваемого кадра как статичной картинки с выбранным форматом/дизерингом/настройками
- **Flash** (только для 2-кадровых GIF) — оба кадра конвертируются в один SCR с атрибутами FLASH. Ячейки, отличающиеся между кадрами, используют бит FLASH (0x80) для чередования ink↔paper каждые 320мс; одинаковые ячейки остаются статичными
- **Animation** — все кадры конвертируются в SCA-анимацию. Задержки кадров из GIF сохраняются (конвертируются в единицы SCA по 20 мс). После импорта доступен полный редактор SCA (плёнка кадров, воспроизведение, обрезка, редактирование задержек, оптимизация, экспорт). Выходной формат зависит от выпадающего списка **Format**:
  - Любой формат кроме 53c и Chunks → SCA type 0 (полные SCR-кадры, 6912 байт на кадр)
  - **53c** → SCA type 1 (кадры только из атрибутов, 768 байт на кадр + встроенный паттерн заливки). Паттерн берётся из селектора **53c Pattern** (Checker, Stripes, DD/77 или Custom)
  - **Chunks 4×4** или **Chunks 4×2** → SCA type 2 (монохромные кадры со сжатием chunks). Каждый кадр дизерится в монохром и сжимается статическим словарём из 4 паттернов. Размер закодированных данных кадра зависит от региона (например, 768 байт для 4×4 на полный экран, 256 для одной трети) — кодовая таблица не хранится покадрово

### Компоновка

Диалог показывает два холста:
- **ORIGINAL** — исходное изображение с выбором масштаба (Fit, x1–x4) и флажком **Grid** для наложения сетки 8×8 пикселей внутри области обрезки. Fit масштабирует для заполнения доступного пространства, максимум x2.
- **PREVIEW** — результат дизеринга под ZX Spectrum с выбором масштаба (Fit, x1–x5, по умолчанию x2) и флажком **Grid** для наложения сетки атрибутов 8×8.

Оба холста показывают полосы прокрутки, когда увеличенное изображение не помещается в доступное пространство.

### Вкладка Image

#### Source (источник)

Обрезка исходного изображения:
- **X, Y, W, H** — прямоугольник обрезки
- **Reset** — сбросить к исходному размеру
- **Full** — использовать полное изображение
- **Detect** — автоопределение области 256×192
- **4:3** — зафиксировать соотношение сторон 4:3
- **Colors** — выбор цветового пространства для перцептуального сопоставления: **RGB** (взвешенное евклидово), **LAB** (CIE76 Delta E) или **OkLab** (по умолчанию; Björn Ottosson 2020, более равномерное для синих/фиолетовых). Отключается при активном Mono output
- **Pair fit** — стратегия выбора лучшей пары цветов ink/paper для каждой ячейки/блока:
  - **Best fit** — полный перебор всех пар палитры с минимизацией суммарного расстояния ближайшего цвета на пиксель. Оригинальный алгоритм по умолчанию
  - **Blend fit** — оценивает каждую пару проецированием каждого пикселя на отрезок между ink и paper, измеряя перпендикулярное расстояние. Лучше моделирует смешивание двух цветов при дизеринге, часто даёт более гладкие результаты
  - **PCA gradient** — использует метод главных компонент (PCA) для определения доминирующей цветовой оси в каждой ячейке, затем тестирует лучшие палитровые соответствия вблизи крайних точек оси с метрикой проекции на линию. Хорошо для ячеек с выраженным цветовым градиентом
  - Работает со всеми размерами блоков ZX (8×8, 8×2, 8×1, 8×4) и ULA+. Скрыт для форматов без ink/paper (NXI, SL2, LoRes). Отключается при активном Mono output
- **Grayscale** — преобразовать в оттенки серого перед обработкой
- **Mono output** — вывод только чёрно-белый (ink 0, paper 7, bright). Работает со всеми форматами: SCR, IFL, MLT, BSC, BMC4, GMX, Gigascreen, Gigaattr, GAP, HLR, STL, RGB3, ULA+, GMX 160×200, 53c, SPECSCII, NXI, SL2, LoRes, Radastan, ZXP

#### Transform (преобразование)

- **Fit mode:**
  - Stretch — растянуть до заполнения (может исказить)
  - Letterbox — вписать в цель с полосами
  - Fill/crop — заполнить цель, обрезать лишнее
  - Fit width — подогнать по ширине
  - Fit height — подогнать по высоте

- **Align:** Top-Left, Top, Top-Right, Left, Center, Right, Bottom-Left, Bottom, Bottom-Right

- **Алгоритмы дизеринга:**

  Cell-Aware (с учётом атрибутов):
  - Cell Floyd, Cell Atkinson, Cell Serpentine, Cell Sierra 2, Cell Riemersma, Cell Ordered, Cell Blue Noise, Cell Pattern, Cell None

  Global (классические):
  - Floyd-Steinberg, Jarvis, Stucki, Burkes, Sierra, Sierra Lite, Sierra 2-Row, Serpentine, Dizzy, Riemersma, Atkinson, Ordered 2×2, Ordered 4×4, Ordered 8×8, Blue Noise, a-dither, Pattern, Noise, None

  По умолчанию дизеринг — **None** (ближайший цвет, без дизеринга).

  - Ползунок **Strength** (0–100%) — регулирует, сколько ошибки квантования распространяется при диффузии ошибки. 0 = чистое квантование (без диффузии), 100 = классическая полная диффузия. Для методов ordered / pattern / blue-noise / a-dither значение Strength > 0 включает гибридный режим ordered + диффузия (упорядоченный порог используется как смещение, а остаток ошибки диффундируется ядром Флойда-Стейнберга).
  - Флажок **Serpentine scan** — чередует направление обхода строк во время диффузии ошибки (слева направо по чётным строкам, справа налево по нечётным), уменьшая горизонтальные полосы. Применяется ко всем глобальным методам с диффузией ошибки.
  - Ползунок **Pre-blur** (0–50) — размывает пиксели ячейки/блока перед выбором пары цветов для стабилизации атрибутов на шумных или детализированных изображениях. Использует 3-проходное сепарабельное прямоугольное размытие (аппроксимация гауссова) с зажимом по краям. Большие значения дают более однородные цвета атрибутов. Битовая карта (bitmap) всегда генерируется по исходным (неразмытым) пикселям, поэтому пиксельная детализация сохраняется. Работает со всеми размерами блоков ZX: 8×8 (SCR), 8×2 (IFL), 8×1 (MLT), 8×4 (BMC4).
  - **Dizzy** (Liam Appelbe, 2023) — диффузия ошибки с динамическим знаменателем: для каждого пикселя суммируются веса необработанных соседей в пределах изображения (ортогональные = 1.0, диагональные = 0.1), и `error × weight / denom` распределяется пропорционально. Ошибка не теряется на краях; получаются паттерны, близкие к голубому шуму.
  - **a-dither** (арифметический дизеринг, формула FFmpeg `((x + y·236) · 119) & 0xff`) — хеш-порог на пиксель. Пространственно стабильный, близкий к голубому шуму, без таблиц поиска.

- **Dither Regions** (вкладка **Dither**) — рисование лассо-регионов на исходном или превью-холсте для назначения разных алгоритмов дизеринга и силы дизеринга разным областям. До 3 цветных регионов (красный, зелёный, синий), каждый со своим выпадающим списком метода дизеринга и **ползунком силы** (0–100%). Последние использованные методы и силы сохраняются в localStorage (по умолчанию: Floyd-Steinberg, Ordered 2×2, Riemersma; все 100%). Управление:
  - **Радиокнопки регионов** (1/2/3) — выбор региона для рисования. Каждый имеет цветовую метку, список метода и ползунок силы.
  - **Ползунок силы** (0–100) — управляет интенсивностью дизеринга для региона независимо от основного ползунка Strength.
  - **Erase** — режим стирания: удаляет пиксели региона (возврат к дизерингу по умолчанию).
  - **Show** — видимость оверлея: Source, Preview, Both или None. Переключение этого списка только перерисовывает оверлей без повторной конвертации.
  - **Clear All** — удаление всех регионов и сброс маски.
  - **Рисование лассо:** переключитесь на вкладку **Dither**, затем кликайте на исходном или превью-холсте для размещения вершин полигона. Двойной клик или клик рядом с первой вершиной закрывает полигон. Правый клик или **Escape** отменяет текущий полигон. Вкладка **Image** — для управления кропом; вкладка **Adjustments** — для настройки цвета/уровней; вкладка **Dither** — для рисования лассо.
  - При конвертации каждая ячейка (для ячеечных форматов) или пиксель (для попиксельных форматов) использует метод и силу дизеринга своего региона; неназначенные области используют основной выпадающий список Dither и глобальный ползунок Strength. Если несколько регионов перекрывают одну ячейку, побеждает регион с наибольшим количеством пикселей в этой ячейке. Одинаковый метод с разной силой создаёт отдельные проходы конвертации.
  - **Производительность:** при перетаскивании любого ползунка настройки многопроходная композиция регионов пропускается для отзывчивости превью. Полнокачественный рендер со всеми регионами выполняется при отпускании ползунка.
  - Поддерживаемые форматы: SCR, IFL, MLT, BSC, BMC4, RGB3, Gigascreen/MG, GMX 640, ULA+, MLT+ULA+, ZXP ULA+, NXI (256, 320, 640), SL2 (256, 320, 640), LoRes, LoRes RAD.
  - Маска сбрасывается при смене формата или загрузке нового изображения.

- **Paper** — правило назначения цветов ink и paper в каждой ячейке (не применяется к формату ULA+, который использует независимые половины CLUT):
  - **Darker color** — по умолчанию; более тёмный из двух цветов ячейки становится paper (для каждой ячейки отдельно, по перцептуальной яркости). Для одноцветных ячеек (ink и paper одинаковые) цвет сравнивается с серединой: тёмные цвета становятся paper (биты 0), светлые — ink (биты 1)
  - **Lighter color** — более светлый из двух цветов ячейки становится paper. Для одноцветных ячеек цвет сравнивается с серединой: светлые цвета становятся paper (биты 0), тёмные — ink (биты 1)
  - **First pixel paper** — цвет левого верхнего пикселя в каждой ячейке становится paper; полезно для спрайтшитов, где угловой пиксель рамки определяет цвет фона

#### Output (выход)

- **Format:** SCR, ULA+, 53c (attr), IFL (8×2), BMC4 (8×4), MLT (8×1), MLT+ULA+, BSC, BSP, RGB3, Gigascreen, Gigaattr, GAP, GAP Dual, GA Pattern, GAP Pattern, GAP Dual Pattern, HLR, STL, GMX 640×200, GMX 160×200, NXI (256×192, 320×256, 640×256), SL2 (256×192, 320×256, 640×256), SPECSCII, Mono, Mono 2/3, Mono 1/3, LoRes, Radastan, ZXP, chr$, btile (Nirvana 16×16), wtile (Nirvana 24×16)
- Выбор **Palette**
- **Palette strip** — кликабельная полоса из 16 цветных образцов (8 обычных + 8 ярких) под выпадающим списком Palette. Клик по образцу отключает этот цвет; отключённые цвета помечаются белым крестом (X) и исключаются из поиска пар ink/paper во всех размерах блоков и из поиска ближайшего цвета. Минимум 2 цвета должны оставаться включёнными. Сбрасывается при смене палитры или формата. Скрывается для не-ZX форматов (ULA+, NXI, SL2, LoRes)
- **53c Pattern** (для формата 53c): Checker, Stripes, DD/77, Custom. При выборе «Custom» появляется поле ввода hex, где можно задать 8 байт через пробел (например `0F 0F 0F 0F F0 F0 F0 F0`). Каждый байт определяет одну строку паттерна 8×8 (MSB = левый пиксель). Пользовательский паттерн используется как для импорта одиночного изображения в 53c, так и для импорта анимированного GIF → SCA type 1
- **GA Pattern** (для форматов GA/GAP pattern): выпадающий список паттерна заливки — выбор 8×8 тайла паттерна для битмапа. Те же пресеты, что в HLR (solid, top-bottom, left-right, checker, stripes, diagonal, DD/77 и др.). Паттерн фиксирован для всей картинки; инструменты рисования изменяют только атрибуты (цвета ink/paper)
- **SPECSCII Charset** (для формата SPECSCII): Full (ROM-шрифт + блочная графика, 112 глифов), ASCII (только ROM-шрифт, 96 глифов), UDG (только блочная графика, 16 глифов + пробел)
- **ULA+ Palette:** Auto, Load .pal, From picture
- **Position:** X, Y смещение
- **Size:** W, H (с фиксацией соотношения сторон). Для btile/wtile значения привязываются к кратным размеру тайла при Enter/blur (16px для btile, 24px ширина / 16px высота для wtile)
- **Tile to screens** — разбить обрезанный источник на сетку картинок:
  - Каждый тайл — один полный выходной формат (например 256×192 для SCR)
  - Размер сетки рассчитывается автоматически (столбцы × строки = всего картинок)
  - Крайние тайлы дополняются чёрным
  - Имена файлов: `filename_col_row.ext`
  - Жёлтая пунктирная сетка на холсте оригинала показывает границы тайлов
  - Кнопки ◄/► переключают между тайлами; предпросмотр показывает реальный результат конвертации каждого тайла
  - Элементы Position/Size/Fit/Align отключаются при включённом тайлинге

### Вкладка Adjustments (настройки)

| Параметр | Диапазон |
|----------|----------|
| Contrast | -100 — +100 |
| Brightness | -100 — +100 |
| Saturation | -100 — +100 |
| Gamma | 0.20 — 3.00 |
| Sharpness | 0 — 100 |
| Smoothing | 0 — 100 (билатеральный фильтр — снижение шума с сохранением краёв) |
| Levels | Black point (0-127), White point (128-255) |
| Color balance | Каналы R, G, B (-50 — +50 каждый) |

**Reset** — сброс всех настроек к значениям по умолчанию.

### Нижние элементы

- **Zoom:** x1, x2, x3
- **Grid** — показать сетку 8×8 на превью
- **Выпадающий список режима** (только для анимированных GIF) — выбор режима импорта: Picture, Flash или Animation
- **Навигация по кадрам** (только для анимированных GIF) — кнопки ◄/► и ползунок для просмотра любого отдельного кадра с текущими настройками формата, дизеринга и коррекции. Метка показывает номер текущего кадра и общее количество (например «3 / 48»). Режим Animation по-прежнему импортирует все кадры вне зависимости от просматриваемого
- Кнопки **Cancel** / **Import**

![Диалог импорта изображений](screenshots/image_import.png)

### Сравнение A/B

Диалог импорта включает функцию сравнения A/B для оценки различных настроек конвертации бок о бок.

**Кнопки просмотра** (правая сторона панели вкладок):
- **A** — показать превью слота A (слот A активен для редактирования)
- **B** — показать превью слота B (слот B активен для редактирования)
- **A+B** — показать оба превью рядом; клик по панели выбирает её для редактирования (выделяется синей рамкой)

**Индивидуальные настройки** — каждый слот независимо хранит: метод и силу дизеринга, serpentine scan, формат, палитру, отключённые цвета, цветовое пространство, стратегию цвета, pre-blur, правило бумаги, все настройки коррекции (контраст, яркость, насыщенность, гамма, резкость, сглаживание, уровни, цветовой баланс), флаги grayscale/mono, маски регионов дизеринга и специфичные для формата параметры (палитра ULA+, тип ZXP, паттерн 53c, паттерн HLR, режим chr, набор символов specscii).

**Общие настройки** — область обрезки, режим вписывания и выравнивание влияют на оба слота одинаково. Их изменение перерисовывает оба превью.

**Кнопка Copy A→B** — копирует все настройки активного слота в другой. Надпись обновляется в зависимости от направления (например «Copy B→A», когда активен B).

**Авто-метки** — под каждой панелью превью отображается краткая сводка ключевых настроек слота (например «Floyd · SCR · Blend · blur:12 · C:+10»).

**Zoom и Grid** — эти элементы управления общие и влияют на оба превью одновременно.

**Import** — импортирует результат текущего активного слота (того, чьи настройки показаны во вкладках ниже).

---

## 19. SCA Animation Editor — редактор анимации SCA

SCA Editor открывается как полноэкранное наложение при редактировании `.sca`-файлов.

### Верхняя панель

- **← Back** — вернуться к основному виду
- **Save As...** — выпадающий список с вариантами экспорта: SCA, SCR zip, 53c zip, GIF (анимированный), PNG zip
- **Save** — сохранить в выбранном формате

При сохранении в формате **SCA** появляется окно настроек с параметрами:
- **Compression** — выбор сжатия для SCA type 2: Uncompressed, ZX0, Laser Compact или RLE. RLE использует формат контрольных байтов (PackBits-стиль), оптимизированный для быстрой распаковки на Z80 (~23 байта распаковщик). Коэффициент сжатия находится между несжатым и ZX0/LC, но скорость распаковки значительно выше (LDIR для литералов, DJNZ для повторов). Анимации Chunks 4×4/4×2 всегда сохраняются как type 2 со сжатием chunks (дополнительный выбор сжатия не требуется)
- **Region** — выбор области экрана для кадров type 2: верхняя, средняя, нижняя треть, верхняя+средняя, средняя+нижняя или полный экран. Применяется ко всем типам сжатия, включая Chunks. Для Chunks размер закодированных данных масштабируется с регионом (1 байт на ячейку для 4×4, 2 байта на ячейку для 4×2)
- **ASM (zip)** — экспорт плеера SCA как исходника sjasmplus с данными кадров в ZIP-архиве. Поддерживает type 0 (полные кадры), type 1 (только атрибуты с паттерном заливки) и type 2 со сжатием chunks (встраивает статическую таблицу поиска и распаковщик DeChunks). Плеер выбирает ROM 1 при запуске для совместимости со 128K

### Кинолента (Filmstrip)

Прокручиваемая полоса миниатюр кадров. Кликните для выбора кадра. **Ctrl+Click** или **Delete/Backspace** переключает ручное удаление выбранного кадра. Отмеченные кадры показывают всплывающую подсказку с их статусом (обрезан / удалён вручную / дубликат) и пометкой, что они будут исключены из сохранённого файла.

### Боковая панель

#### Playback (воспроизведение)
- **|◀◀** — перейти к началу
- **|◀** — предыдущий кадр
- **▶** — воспроизведение/пауза
- **▶|** — следующий кадр
- **▶▶|** — перейти к концу

Информация о кадре: номер кадра, общее количество, задержка в мс.

#### Preview Zoom
- x1, x2, x3

#### Trim from Start / Trim from End (обрезка)
Регулировка обрезки кнопками **−** / **+** для удаления кадров с начала или конца.

#### Frame Delay (задержка кадра)
- Настройка значения задержки кнопками **−** / **+**
- **Apply to Current** — установить задержку для текущего кадра
- **Apply to All** — установить задержку для всех кадров

#### Optimize (оптимизация)
- **Remove Duplicate Frames** — удаление последовательных дубликатов с объединением задержек
- **Remove loop frame** — опциональное удаление последнего кадра, если он совпадает с первым
- **Reset** — восстановление исходных задержек

#### Result (результат)
Статистика:
- Original frames / Trimmed frames / Duplicates
- Original duration / Trimmed duration
- Original size / Trimmed size

#### Preview Mode
- **All frames** — воспроизведение всех кадров
- **Trimmed only** — воспроизведение только необрезанных кадров

### Управление просмотром SCA

При просмотре (не редактировании) SCA-файла под основными вкладками появляются упрощённые элементы:

- Кнопки **< / >** — навигация по кадрам
- Кнопка **Play** — старт/стоп воспроизведения
- **Ползунок кадров** — прокрутка по кадрам
- Индикатор кадра

![Редактор SCA](screenshots/sca_editor.png)

---

## 20. Memory Viewer — просмотрщик памяти (плавающая панель)

Доступен после загрузки файла снапшота (`.sna`, `.z80`). Просмотр сырой памяти как 1-битной графики.

### Холст дампа

Холст 256×384 пикселей, показывающий содержимое памяти как монохромный битмап.

### Элементы управления

- **Bank** (для снапшотов 128K) — выбор банка памяти 0-7
- **Address** — начальный адрес (0-16383)
- **Навигация:**
  - **< B / B >** — перемещение на 1 байт
  - **< Line / Line >** — перемещение на 1 линию
  - **< Row / Row >** — перемещение на 8 линий (1 строка символов)
  - **< Spr / Spr >** — перемещение на 1 спрайт
  - **< Page / Page >** — перемещение на 1 страницу
- **Width** — ширина отображения в байтах (1-8)
- **Height** — высота отображения в строках (1-64)
- **Invert** — инвертировать отображение
- **Grid** — показать наложение сетки
- **Char mode** — знакоместо-выровненное отображение
- **Zoom** — x1, x2, x3, x4

### Preview (предпросмотр)

Миниатюрный предпросмотр выделенной области спрайта.

### Grab (захват)

Захват спрайтов из просмотрщика памяти в список спрайтов:
- Кнопка **Grab** — захватить отображаемый спрайт
- **Mode:** Single sprite, Sprite phases, Singles grid, Phases grid
- Настройки сетки: размер по ячейкам или количеству, столбцы/строки, порядок

![Просмотрщик памяти](screenshots/memory_viewer.png)

---

## 21. Display Filters — фильтры отображения

Расположены во вкладке Tools (раскрываемая секция). Нажмите заголовок «Display Filters» для раскрытия. Установите флажок **On** для включения эффектов постобработки.

| Фильтр | Описание |
|--------|----------|
| Scanlines | Гауссов профиль луча, имитация строчной развёртки ЭЛТ |
| Noise | Статический шум плёнки с опциональной анимацией |
| Composite | Размытие цвета, имитация композитного видеосигнала |
| Phosphor Glow | Свечение люминофора |
| Vignette | Затемнение по краям экрана |
| CRT Curvature | Бочкообразное искажение, имитация кривизны ЭЛТ |
| Pixel Smoothing | Билинейная интерполяция для сглаживания пикселей |

**Пресеты:** None, CRT TV, Composite, VHS, Arcade

Настройки сохраняются в localStorage и файлах workspace (.slw).

---

## 22. QR Code Generator — генератор QR-кодов

Открывается из вкладки Tools → кнопка QR Code.

- **Text or URL** — поле ввода (автоматически преобразуется в верхний регистр для максимальной ёмкости)
- **Size (version):** Auto или V1 (21×21, 20 букв) — V20 (97×97, 970 букв)
- **Module size:** 1, 2, 3, 4 или 8 пикселей
- **Position:** координаты X, Y
- Кликните на холсте для размещения сгенерированного QR-кода

---

## 23. Полноэкранный режим

Нажмите **F11** для переключения полноэкранного режима из любой вкладки — просмотрщика или редактора.

- **Просмотр (Viewer)** — показывает только холст с активными фильтрами дисплея; без боковой панели и плавающей палитры
- **Редактор (Editor)** — показывает холст с плавающей палитрой инструментов и панелью предпросмотра
- **Tab** — показать/скрыть плавающую палитру (только в полноэкранном режиме редактора)
- **Escape** — выход из полноэкранного режима

Плавающая палитра включает все инструменты рисования, инструменты выделения/буфера обмена, цветовую палитру и переключатель Bright. Она отображается только при входе в полноэкранный режим из вкладки Edit.

---

## 24. Справочник горячих клавиш

### Просмотрщик (Viewer)

| Клавиша | Действие |
|---------|----------|
| 1-5 | Установить масштаб |
| Ctrl+колесо | Масштабирование |
| Стрелки | Прокрутка холста при увеличении |
| F | Переключить Flash-анимацию |
| G | Циклический выбор размера сетки (None/8/16/24px) |
| ~ | Переключить панель предпросмотра |
| Space | Воспроизведение/Пауза (SCA-анимация) |
| Left/Right | Пред./след. кадр (SCA) |
| F11 | Полноэкранный режим |
| Escape | Выход из полноэкранного режима |

### Редактор — Инструменты

| Клавиша | Действие |
|---------|----------|
| P | Pixel — свободное рисование |
| L | Line — линии |
| R | Rectangle — прямоугольники |
| O | Circle/Ellipse — окружности/эллипсы |
| G | Airbrush — аэрограф |
| D | Gradient — градиент |
| I | Flood Fill — заливка |
| C | Fill Cell — заливка знакоместа |
| A | Recolor — перекраска (только атрибуты) |
| E | Eraser — ластик |
| T | Text — текст |
| K | Color Picker (захват цвета) |
| J | Cell Invert — инверсия ячейки |
| W | Dither Brush — кисть передизеринга |
| S | Select — выделение |

### Редактор — Рисование

| Клавиша | Действие |
|---------|----------|
| Левый клик | Рисование ink |
| Правый клик | Рисование paper |
| Alt+клик | Пипетка (захват ink+paper из ячейки) |
| B | Переключить Bright |
| F | Переключить Flash |
| X | Поменять ink/paper местами |
| [ / ] | Уменьшить/Увеличить размер кисти |
| ` (обр. апостроф) | Переключить предпросмотр кисти |

### Редактор — Редактирование

| Клавиша | Действие |
|---------|----------|
| Ctrl+C | Копировать выделение |
| Ctrl+X | Вырезать выделение |
| Ctrl+V | Вставить |
| R / H / V | Повернуть/Отразить буфер (при вставке) |
| N | Инвертировать выделение (обмен ink ↔ paper) |
| Shift+W | Передизеринг выделения текущими настройками кисти |
| Ctrl+Z | Отмена (до 32 уровней) |
| Ctrl+Y | Повтор |
| Ctrl+S | Сохранить |
| F11 | Полноэкранный режим |
| Tab | Плавающая палитра (в полноэкранном режиме) |
| Escape | Отмена выделения/вставки, выход из полноэкранного режима |

### Модификаторы фигур (Rectangle, Circle/Ellipse)

| Клавиша | Действие |
|---------|----------|
| Ctrl | Квадрат/круг (1:1) |
| Alt | Рисование от центра |
| Ctrl+Alt | Квадрат/круг от центра |

### Пользовательские кисти

| Действие | Эффект |
|----------|--------|
| Клик на слоте | Выбрать (пустой → начать захват) |
| Shift+клик | Захватить с экрана |
| Ctrl+клик | Очистить слот |

### Мышь

| Действие | Эффект |
|----------|--------|
| Перетаскивание | Прокрутка холста (при увеличении, в режиме просмотра) |
| Перетащить файл | Загрузить изображение |

> **Примечание:** горячие клавиши работают с любой раскладкой клавиатуры (русской, немецкой и др.) — они привязаны к физическому расположению клавиш.

---

## 25. Редактор шрифтов (Font Editor)

Редактор шрифтов — отдельная страница (`font_editor.html`) для создания и редактирования растровых шрифтов ZX Spectrum. Поддерживает фиксированные шрифты 8×8 и пропорциональные шрифты FZX. Открывается по ссылке **Font** в нижней панели вкладки View, или напрямую через `font_editor.html`.

### Элементы управления в заголовке

- **Load** — открыть файл шрифта (`.768`, `.ch8`, `.bin`, `.SpecCHR`, `.fnt`, `.fzx`)
- **Save** — контекстное сохранение. Действие зависит от текущего количества глифов: 96 глифов сохраняет `.768` напрямую; 21 глиф — `.udg`; 256 глифов — диалог Normal/Interlaced; 117 глифов (96+21) — выбор из Single file, Font+UDG или UDG+Font; >256 глифов — диалог экспорта диапазона; остальные количества сохраняют `.bin`. FZX-шрифты сохраняются как `.fzx` напрямую.
- **Append** — загрузить файл шрифта и добавить его глифы после существующих (до 1024 всего)
- **New ▾** — создать новый шрифт: 96 глифов, 256 глифов, произвольное количество (до 1024), «взорванный» (256 чересстрочных), или новый FZX-шрифт
- **?** — показать горячие клавиши и справку
- **Light / Dark** — переключение темы (синхронизируется с SpectraLab через `localStorage`)

### Сетка глифов (левая панель)

Отображает все глифы в масштабе 4× в сетке из 16 столбцов. Щёлкните по глифу для выбора. Для FZX-шрифтов сетка использует flexbox-раскладку с ячейками переменной ширины.

- Подсказка при наведении показывает индекс глифа, hex-код и привязанный символ
- При ширине, отличной от 8, неактивные столбцы затемняются
- Флажок **Grid** включает/выключает отображение сетки пикселей
- Флажок **Labels** показывает привязанный символ под каждым глифом в сетке

### Инструменты рисования

Панель инструментов над редактором содержит пять инструментов:

| Инструмент | Кнопка | Клавиша | Левая кнопка мыши | Правая кнопка мыши |
|------------|--------|---------|-------------------|-------------------|
| Пиксель | ✎ | P | Переключение (XOR) | Стирание |
| Линия | ╱ | L | Рисование линии (установка) | Стирание линии |
| Прямоугольник | ☐ | R | Контур | Заливка |
| Круг | ○ | O | Контур | Заливка |
| Ластик | ⌫ | E | Стирание от руки | Стирание прямоугольной области |

**Фигурные инструменты** (линия, прямоугольник, круг): нажмите для установки начальной точки, перетащите для предпросмотра фигуры (полупрозрачное наложение), отпустите для подтверждения. Перемещение мыши за пределы холста отменяет фигуру. Правая кнопка ластика работает аналогично — выделите область, отпустите для очистки.

**Инструменты свободного рисования** (пиксель, ластик): нажмите и перетаскивайте для непрерывного рисования. Ластик использует интерполяцию Брезенхэма для плавных штрихов без пропусков.

Все инструменты работают как в обычном (8×8), так и в FZX-режиме, с учётом активных столбцов, защиты строки 0 (режим переменной ширины) и флажка «Whole font».

### Редактор пикселей (центральная панель)

Выбранный глиф отображается в крупном масштабе (50× для 8×8, динамический размер для FZX — вписывается в ~400 пикс.). Щёлкните или перетащите курсор для рисования активным инструментом.

- **Invert** — инвертировать все биты глифа (клавиша **I**)
- **Clear** — обнулить все биты (клавиша **Delete**)
- **Whole font** — применять Invert/Clear/трансформации и редактирование пикселей ко всем глифам сразу. В режиме переменной ширины учитывается ширина каждого глифа.
- В режиме переменной ширины строка 0 (байт ширины) защищена от редактирования. Флажок **Hide W** (по умолчанию: включён) скрывает её в сетке, редакторе и текстовом превью.

### Колонка управления (справа от превью)

- **Glyphs** — количество глифов (1–1024). Работает как для фиксированных, так и для FZX-шрифтов. Изменение количества сохраняет существующие данные глифов и привязку символов.
- **→ FZX** / **→ Fixed** — конвертация шрифта между фиксированным и пропорциональным FZX-форматом. Fixed→FZX вычисляет визуальные границы каждого глифа, устанавливает ширину по фактическому содержимому и выравнивает растр влево. FZX→Fixed обрезает до 8×8 с учётом сдвига.

### Сдвиг (с переносом)

Кнопки-стрелки сдвигают пиксели глифа с переносом (вытесненные пиксели появляются с противоположной стороны).

### Трансформации

Выпадающий список содержит 22 операции, применяемых к выбранному глифу или ко всему шрифту:

| Группа | Трансформации |
|--------|--------------|
| Жирный | Bold Right, Bold Left, Bold Down |
| Курсив | Italic 1/2/3 Right, Italic 1/2/3 Left |
| Сдвиг (заполнение нулями) | Shift Right, Left, Up, Down |
| Отражение / Поворот | Flip Horizontal, Flip Vertical, Rotate 90° CW, 90° CCW, 180° |
| Выравнивание | Align Left, Align Right, Align Top, Align Bottom |

Выравнивание сдвигает пиксели глифа, пока крайний непустой столбец/строка не коснётся соответствующего края. Работает для фиксированных и FZX-глифов.

### Режим ширины

Выбор количества активных бит в строке глифа:

| Режим | Активные биты | Ширина в пикселях |
|-------|--------------|-------------------|
| 8 (full) | Биты 7–0 | 8 |
| 6 (left) | Биты 7–2 | 6 |
| 6 (right) | Биты 5–0 | 6 |
| 4 (left) | Биты 7–4 | 4 |
| 4 (right) | Биты 3–0 | 4 |
| Variable | Первый байт = ширина | 1–8 |

Неактивные столбцы затемняются как в сетке, так и в редакторе пикселей. При переключении с 4 (right) или 6 (right) в режим Variable байты ширины устанавливаются в соответствующее значение (4 или 6). Флажок **Hide W** управляет видимостью байта ширины (строка 0) в режиме Variable.

### Привязка символов

- Поле **Characters** + кнопка **Map** — привязать строку символов, начиная с выбранного глифа
- **Clear** — удалить привязку для текущего режима ширины
- **From (Cyr) / To (Lat)** — переназначение символов (например, кириллица на латинские аналоги)

### Экспорт/импорт метрик

- **Export .metrics** — сохранить привязки, режим ширины, ремап и метаданные шрифта в JSON
- **Import .metrics** — загрузить ранее экспортированный файл `.metrics`

### Текстовый образец (Text Sample)

Живой предпросмотр шрифта, отображающий три панграммы. Текст переносится посимвольно, заполняя доступную ширину панели, и перестраивается при изменении размера окна. Каждое предложение остаётся на своей строке (с переносом внутри).

- **Zoom** — масштаб пикселей 1×, 2× или 3×
- **Grid** — показывать промежутки между пикселями глифов (при масштабе > 1×)
- **Uppercase** — отображать весь текст заглавными буквами
- **Timex** — отображение с соотношением сторон пикселя 2:1 (пиксели половинной ширины), имитирующее режим Timex hi-res 512×192

### Копирование / Вставка глифов

- **Ctrl+C** — копировать выбранный глиф в буфер обмена
- **Ctrl+V** — вставить глиф поверх выбранного
- Работает внутри одного режима и между fixed/FZX режимами. Кросс-форматная вставка автоматически конвертирует: fixed→FZX подбирает ширину и раскладку, FZX→fixed обрезает до 8×8.

### Отмена / Повтор

Редактор шрифтов поддерживает до 50 уровней отмены. Отменяемы все модификации: редактирование пикселей, трансформации, инверсия, очистка, изменение количества глифов, ширины, а также все свойства FZX (высота, трекинг, ширина глифа, сдвиг, кернинг).

- **Ctrl+Z** — отмена
- **Ctrl+Y** или **Ctrl+Shift+Z** — повтор
- Загрузка нового файла шрифта или создание нового шрифта очищает историю отмен
- Отмена работает при переключении между режимами (фиксированный ↔ FZX)

### Пропорциональные шрифты FZX

FZX — формат пропорциональных шрифтов ZX Spectrum с переменной шириной глифов (1–16 пикс.), настраиваемой высотой (1–16 пикс.), сдвигом и кернингом для каждого глифа, а также знаковым трекингом. В режиме FZX:

- Управление для каждого глифа: **Width**, **Shift**, **Kern**
- Управление для всего шрифта: **Height**, **Tracking**
- Сетка глифов использует гибкую раскладку, отражающую реальную ширину
- Все трансформации адаптированы для растровых изображений переменной ширины

### Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| Ctrl+Z | Отмена |
| Ctrl+Y / Ctrl+Shift+Z | Повтор |
| Ctrl+C | Копировать глиф |
| Ctrl+V | Вставить глиф |
| Стрелки | Навигация по глифам в сетке |
| P | Инструмент «Пиксель» |
| L | Инструмент «Линия» |
| R | Инструмент «Прямоугольник» |
| O | Инструмент «Круг / Эллипс» |
| E | Инструмент «Ластик» |
| B | Жирный вправо |
| I | Инвертировать глиф / шрифт |
| Delete | Очистить глиф / шрифт |

### Визуальный выбор формата

При загрузке файла шрифта размером 2048 байт появляется модальное окно с превью обоих вариантов интерпретации (обычный и чересстрочный) — по 16 глифов в строке × 4 строки. Щёлкните по нужному варианту для загрузки.

### Поддерживаемые форматы шрифтов

| Размер | Глифов | Формат |
|--------|--------|--------|
| 768 байт | 96 | Стандартный шрифт ZX Spectrum (ASCII 32–127) |
| 2048 байт | 256 | Полный 256-символьный шрифт |
| 2048 байт | 256 | «Взорванный» формат (чересстрочный) |
| 1–1024 × 8 байт | Произвольно | Шрифт с произвольным количеством глифов |
| Переменный | Переменно | Пропорциональный шрифт FZX (`.fzx`) |

---

## 26. ZGS-редактор

ZGS-редактор — отдельная страница (`zgs_editor.html`) для редактирования векторных сцен ZGS (ZX Graphics Script), используемых в приключенческих играх для ZX Spectrum. ZGS использует байткод-формат с логической сеткой координат 128x96, отображаемой на экране 256x192 пикселей. Открывается по ссылке **ZGS Editor** во вкладке Tools, или напрямую через `zgs_editor.html`.

### Форматы файлов

| Расширение | Тип | Описание |
|------------|-----|----------|
| .zgs | Бинарный | Байткод ZGS с 10-байтным заголовком (магическое число `ZG`, версия, флаги, библиотека ассетов, смещение сцены), опциональное LZ-сжатие, библиотека ассетов (спрайты и скрипты фигур) |
| .zgt | Текстовый | Человекочитаемое ассемблероподобное представление байткода ZGS |
| .zgp | Проект | Файл многосценного проекта (JSON, v1/v2). Содержит имена и исходный текст всех сцен, индекс активной сцены, а также референсные изображения с настройками отображения для каждой сцены (v2) |

### Элементы управления в заголовке

- **Open** — загрузить файл `.zgs` (бинарный), `.zgt` (текстовый) или `.zgp` (проект)
- **New** — добавить новую пустую сцену, если проект содержит изменённые данные; сбросить проект, если все сцены пустые/стандартные
- **Save** — выпадающее меню с выбором формата:
  - **Save .zgs** — ассемблировать и сохранить активную сцену как бинарный файл `.zgs`
  - **Save .zgt** — сохранить активную сцену как текстовый файл `.zgt`
  - **Save .zgp** — сохранить многосценный проект как файл `.zgp`
  - **Save .asm** — экспорт всех сцен как полного Z80 ассемблерного файла для sjasmplus. Скачивается `.zip` с файлом `.asm`, скомпилированными `.zgs` бинарниками для каждой сцены, словарём упакованного текста (`.zdict`) и бинарными шрифтами (`font_8x8.bin`, `font_6x8.bin`, `font_4x8.bin`). Экспортированный файл содержит конфигурационный блок с 4 точками входа JP и флаги условной компиляции (см. ниже)
- **☾/☀** — переключение светлой/тёмной темы (синхронизируется с основным SpectraLab через `localStorage`)

### Вкладки сцен

Панель вкладок под заголовком показывает все сцены проекта. Нажмите на вкладку для переключения сцены. Каждая сцена имеет свой исходный текст, историю отмены/повтора и скомпилированный бинарник.

- Кнопка **+** — добавить новую сцену
- **Двойной клик** по имени вкладки — переименовать сцену
- Кнопка **×** на вкладке — удалить сцену (минимум 1 должна остаться)

### Конфигурационный блок ASM

Экспортированный `.asm` файл размещает конфигурационный блок с фиксированными адресами, начиная с ORG (по умолчанию 0x8000):

| Смещение | Содержимое | Описание |
|----------|------------|----------|
| +0x00 | `jp show_from_addr` | Очистить экран, нарисовать сцену по `scene_addr`, ждать клавишу |
| +0x03 | `jp show_by_num` | Очистить экран, нарисовать сцену `scene_num` из таблицы, ждать клавишу |
| +0x06 | `jp zgs_clear_screen` | Очистить экран используя атрибут `clear_color` |
| +0x09 | `jp zgs_wait_key` | Ожидание нажатия клавиши |
| +0x0C | `zgs_font_addr dw` | Адрес шрифта 8×8 для 32-кол режима (`font_8x8`, incbin) |
| +0x0E | `zgs_scene_addr dw` | Адрес сцены для `show_from_addr` (изменяемый) |
| +0x10 | `zgs_dict_addr dw` | Адрес словаря упакованного текста (или 0) |
| +0x12 | `scene_num db` | Индекс сцены для `show_by_num` (с 0, изменяемый) |
| +0x13 | `clear_color db` | Байт атрибута для `zgs_clear_screen` (0 = чёрный) |
| +0x14 | `zgs_font_42_addr dw` | Адрес шрифта 6×8 для 42-кол режима (`font_6x8`, incbin) |
| +0x16 | `zgs_font_64_addr dw` | Адрес шрифта 4×8 для 64-кол режима (`font_4x8`, incbin) |
| +0x18 | `scene_count db` | Общее количество сцен |
| +0x19 | `scene_table dw×N` | Адреса каждой сцены |

Для показа конкретной сцены программно: запишите индекс сцены в `scene_num` (ORG+0x12), затем `CALL ORG+3`. Для изменения цвета очистки экрана: запишите байт атрибута в `clear_color` (ORG+0x13).

### Условная компиляция (экспорт ASM)

Экспортированный `.asm` файл содержит восемь флагов `DEFINE` в начале, управляющих компиляцией групп опкодов. Закомментируйте неиспользуемые DEFINE для уменьшения размера бинарника — отключённые функции компилируются в минимальные заглушки `or 1 : ret`, а их подпрограммы, таблицы данных и переменные полностью исключаются.

| DEFINE | Опкоды | ~Байт | Описание |
|--------|--------|------:|----------|
| `ZGS_USE_LINES` | 0x64–0x6A | 443 | Рисование линий, горизонтальных и вертикальных линий |
| `ZGS_USE_RECTS` | 0x6B–0x70, 0x7E | 636 | Контур/заливка прямоугольника, очистка области |
| `ZGS_USE_CIRCLES` | 0x73–0x76 | 612 | Контур/заливка окружности |
| `ZGS_USE_ELLIPSES` | 0x89–0x8C | 800 | Контур/заливка эллипса |
| `ZGS_USE_POLYGONS` | 0x71–0x72 | 666 | Контур/заливка многоугольника |
| `ZGS_USE_FLOOD` | 0x19, 0x77 | 2300 | Заливка (включая 512-байт стек + 768-байт битовая карта посещений) |
| `ZGS_USE_TEXT` | 0x80–0x81 | 180 | set_cursor, print_text |
| `ZGS_USE_PACKED_TEXT` | 0x82 | 724 | print_packed (включая ~520 байт словаря) |
| `ZGS_USE_TEXT_42` | 0x83–0x85 | 968 | set_cursor_42, print_text_42, print_packed_42 (42 колонки, 6 пикселей; включая 768 байт font_6x8.bin) |
| `ZGS_USE_TEXT_64` | 0x86–0x88 | 968 | set_cursor_64, print_text_64, print_packed_64 (64 колонки, 4 пикселя; включая 768 байт font_4x8.bin) |
| `ZGS_USE_STAMPS` | 0x78–0x79 | 146 | Штамп (блит спрайта) |

`ZGS_USE_TEXT`, `ZGS_USE_TEXT_42`, `ZGS_USE_TEXT_64` и `ZGS_USE_ELLIPSES` определяются автоматически по содержимому сцен. Остальные DEFINE активны по умолчанию. Бинарные шрифты включаются условно: `font_8x8.bin` (32-кол) требует `ZGS_USE_TEXT`, `font_6x8.bin` (42-кол) — `ZGS_USE_TEXT_42`, `font_4x8.bin` (64-кол) — `ZGS_USE_TEXT_64`. Пользователь может заменить любой бинарный шрифт собственным дизайном. Когда все графические функции отключены (остаются только текстовые), система координат — обработчики точек/перемещений, `plot_pixel`, математические хелперы (`read_abs`, `read_dshort`, `read_dmed`) и таблицы паттернов/масок — автоматически исключается через внутренний флаг `ZGS_HAS_DRAWING`.

Зависимости: `ZGS_USE_PACKED_TEXT` требует `ZGS_USE_TEXT` (использует `print_one_char`). `ZGS_USE_RECTS` (контур) и `ZGS_USE_POLYGONS` (контур) требуют `ZGS_USE_LINES` (используют `draw_line`). Пользователь должен убедиться, что отключённые опкоды не встречаются в данных сцены.

### Текстовый редактор (левая панель)

Моноширинная текстовая область для редактирования ассемблера `.zgt`. Поддержка клавиши Tab для отступов. Автоматический рендеринг через 500 мс после окончания ввода.

**Категории инструкций:**

| Категория | Примеры |
|-----------|---------|
| Атрибуты | `set_ink white bright`, `set_paper blue`, `set_attr 0x47`, `set_pattern checker`, `set_mode xor`/`set_mode set` |
| Перемещение | `move_abs 10, 20`, `move_short 2, -1`, `move_dmed -5, 3` |
| Рисование | `dot_abs`, `line_dmed`, `hline_chain`, `vline_abs`, `rect_fill_abs`, `circle_outline_abs`, `polygon_fill`, `flood_abs` |
| Пакетные | `dot_batch`, `line_batch`, `rect_fill_batch` |
| Ассеты | `.sub`/`.endsub` (скрипты фигур), `.sprite`/`.endsprite` (растровые спрайты), `call`, `stamp_abs`, `stamp_chain` |
| Текст | `set_cursor col, row`, `print_text "строка"`, `print_packed "строка"`, `set_cursor_42 col, row`, `print_text_42 "строка"`, `print_packed_42 "строка"`, `set_cursor_64 col, row`, `print_text_64 "строка"`, `print_packed_64 "строка"` |
| Управление | `.repeat`/`.endrepeat`, `wait_key`, `clear_region`, `end` |

### Предпросмотр (правая панель)

Холст 256x192 (CSS-масштабирование 2x с пиксельной интерполяцией), отображающий отрендеренную сцену.

- **Render** — принудительный рендеринг сцены
- **Play** — пошаговая анимация рисования (один опкод за тик)
- **Step** — выполнить один опкод
- **Speed** — ползунок задержки анимации (5–500 мс на опкод)
- **Pen** — переключить отображение перекрестия пера
- **Grid** — переключить сетку знакомест 8×8 с разделителями третей экрана

### Строка состояния

Отображает ошибки ассемблера с номерами строк или информацию об успехе (размер файла в байтах, количество опкодов).

### Перетаскивание файлов

Перетащите файл `.zgs`, `.zgt` или `.zgp` на страницу для его открытия.

---

## 27. Плагины — поддержка пользовательских форматов

SpectraLab поддерживает пользовательские плагины для извлечения и записи картинок из произвольных бинарных файлов (например, снапшотов игр). Управление плагинами — во вкладке **Tools** → раздел **Plugins**.

### Два типа плагинов

**JSON-дескрипторы (`.slplugin`)** — программирование не требуется. Расположение картинок описывается как адреса/смещения в JSON-файле. Подходит для извлечения экранов из известных адресов памяти в снапшотах.

**JS-плагины (`.slpluginjs`)** — программируемые плагины с произвольной логикой извлечения/записи. Подходят для сжатых форматов или сложных структур данных. Код пишется на JavaScript внутри JSON-обёртки.

### Загрузка плагина

Откройте файл `.slplugin` или `.slpluginjs` через основную кнопку выбора файлов, или перетащите его на приложение. Расширение файла автоматически определяется и направляется в загрузчик плагинов.

Установленные плагины сохраняются между сессиями (в localStorage).

### Использование плагина

Каждый загруженный плагин показывается строкой с кнопками:

- **Open…** — выбрать файл для открытия через плагин. Извлекает картинки согласно определению плагина и добавляет их в панель вкладок
- **Export** (только JS-плагины) — закодировать/сжать текущую активную картинку через функцию `patch()` плагина и скачать результат
- **×** — удалить плагин

### Сессия плагина

При открытии картинок через JSON-дескриптор над вкладками картинок появляется панель сессии. JS-плагины также могут включить сессию, указав `"session": true` в дескрипторе — это полезно для JS-плагинов, которые извлекают несколько картинок из файла-контейнера и записывают их обратно.

- **Replace Picture…** — заменяет текущую активную картинку файлом `.scr`, выбранным с диска. Данные картинки заменяются сразу; история отмен для этой картинки очищается
- **Save Patched File** — записывает все отредактированные картинки обратно в оригинальный файл (по указанным адресам для JSON-плагинов или через функцию `patch()` для JS-плагинов) и скачивает патченный файл. JS-плагины могут показывать окно прогресса во время сжатия
- **Save Raw** — экспортирует каждую картинку как отдельный файл в исходном формате
- **×** — закрывает сессию (картинки остаются открытыми, но теряют связь с оригинальным файлом)

### Формат JSON-дескриптора

```json
{
  "id": "уникальный_id",
  "name": "Отображаемое имя",
  "version": 1,
  "description": "Краткое описание",
  "fileExtensions": [".sna", ".z80"],
  "pictures": [
    {
      "name": "Имя картинки",
      "format": "scr",
      "source": {
        "addressMode": "z80addr",
        "address": "0x4000",
        "length": 6912
      }
    }
  ],
  "fixups": []
}
```

**Режимы адресации:**

| Режим | Описание |
|-------|----------|
| `z80addr` | Логический адрес Z80 (0x4000–0xFFFF). Автоматически определяет банк памяти: 0x4000–0x7FFF → банк 5, 0x8000–0xBFFF → банк 2, 0xC000–0xFFFF → текущий подключённый банк. Поле `bank` позволяет указать банк явно. Требуется файл .sna или .z80. |
| `offset` | Смещение в байтах от начала файла. Работает с любым бинарным файлом. |

Числовые значения принимают строки hex (`"0x4000"`) или обычные числа (`16384`).

**Fixups** — необязательные байтовые патчи, применяемые после записи картинок:

```json
"fixups": [
  { "addressMode": "offset", "offset": "0xC100", "value": [0, 27] }
]
```

### Формат JS-плагина

```json
{
  "id": "уникальный_id",
  "name": "Отображаемое имя",
  "fileExtensions": [".bin"],
  "type": "js",
  "jsSource": [
    "plugin = {",
    "  extract: function(fileBytes, snapshot) {",
    "    // Возвращает: массив {name, format, data}",
    "    return [{ name: 'Screen', format: 'scr', data: fileBytes.slice(0, 6912) }];",
    "  },",
    "  patch: function(originalBytes, pictures, snapshot) {",
    "    // Необязательно: закодировать/сжать и вернуть Uint8Array или Promise",
    "    return new Uint8Array(pictures[0].data);",
    "  }",
    "};"
  ]
}
```

`jsSource` может быть одной строкой или массивом строк (по одной на строку, объединяются через перевод строки). Формат массива рекомендуется для удобства чтения.

**Необязательное поле:** `"session": true` — включает панель сессии (Save Patched File / Save Raw) для JS-плагинов, которые извлекают картинки из файла-контейнера и записывают их обратно. Без этого JS-плагины работают как автономные кодеки (Open для декодирования, Export для кодирования).

**Параметры:**
- `fileBytes` — `Uint8Array` всего файла
- `snapshot` — объект снапшота `{banks: Uint8Array[8], machineType, pagingByte}` или `null` для файлов, не являющихся снапшотами. Массивы банков — subarray-представления в `fileBytes` (без копирования)
- `pictures` — массив `{name, format, data}` с текущими отредактированными данными экрана

**Возвращаемое значение:** `patch()` может вернуть `Uint8Array` (синхронно) или `Promise<Uint8Array>` (асинхронно). Асинхронный возврат полезен для плагинов, которым нужно обновлять UI во время длительных операций (например, индикатор прогресса). Возвращает `null` для отмены сохранения.

**Пространство имён `SL`** — JS-плагины получают объект `SL` как параметр, предоставляющий доступ к модулям сжатия SpectraLab. Плагины могут проверить доступность через `if (SL.ZX0)` перед использованием:

| Свойство | Модуль | Основные методы |
|----------|--------|-----------------|
| `SL.ZX0` | Компрессор ZX0 | `compress(data)` → `{data, delta}`, `decompress(data)` → `Uint8Array` |
| `SL.ZX7` | Компрессор ZX7 | `compress(data)`, `decompress(data)` |
| `SL.RLE` | RLE (PackBits) | `compress(data)`, `decompress(data)` |
| `SL.ZXSC` | ZXSC (LZF) | `compress(data)`, `decompress(data)` |
| `SL.LC` | Laser Compact | `compress(data)`, `decompress(data)` |
| `SL.UPKR` | UPKR | `compress(data)`, `decompress(data)` |
| `SL.LgK` | Компрессор LgK | `compress(data)`, `decompress(data)` |

Пример использования внутри плагина:
```javascript
"jsSource": [
    "plugin = {",
    "  extract: function(fileBytes) {",
    "    var decompressed = SL.ZX0.decompress(fileBytes);",
    "    return [{ name: 'Screen', format: 'scr', data: decompressed }];",
    "  },",
    "  patch: function(originalBytes, pictures) {",
    "    return SL.ZX0.compress(pictures[0].data).data;",
    "  }",
    "};"
]
```

### Примеры плагинов

Примеры плагинов находятся в каталоге `plugins/`:

- `example.slplugin` — JSON-дескриптор: извлекает основной и теневой экраны из 128K .sna снапшота
- `example_js.slpluginjs` — JS-плагин: демонстрирует извлечение и запись с доступом к банкам памяти
- `rle_scr.slpluginjs` — JS-плагин (кодек): загрузка и сохранение SCR-файлов со сжатием RLE (кодирование 0x00/0xFF + счётчик)
- `maria_sna.slpluginjs` — JS-плагин (с сессией): извлекает загрузочный экран + 5 сжатых экранов (ZX0/RLE, автоопределение) из снапшота Maria's Christmas Box 48K .sna. При сохранении патчит Z80-депакер в снапшоте: заменяет встроенный RLE-цикл по адресу `$9602` на `CALL $FE90`, записывает 112-байтный депакер `dzx0_smartRCS` по адресу `$FE90` (конец памяти), применяет RCS-перестановку bitmap перед ZX0-сжатием. Блок данных по адресу `$A000` (заголовок + сжатые экраны) остаётся портабельным для загрузки в другие программы. При недоступности `SL.ZX0` использует RLE. ZX0, RCS и `dzx0_smartRCS` разработаны Einar Saukas. Демонстрирует патчинг Z80-кода, релокацию депакера, работу с данными через границу банков, использование пространства имён `SL` и защиту от переполнения памяти
- `heroquest_128k.slpluginjs` — JS-плагин (с сессией): извлекает 11 графических блоков из снапшота Hero Quest 128K .sna из банков 3, 4, 6 и 7. Десять блоков 128×64 пикселей в банках 3/4/7 хранятся в линейном формате (bitmap + атрибуты) и конвертируются в/из ZX-чередованного SCR; банк 6 содержит полноэкранное игровое поле 256×192 в стандартном формате SCR. Неиспользуемая область экрана заполняется шахматной заливкой из ярких/обычных белых атрибутов для обозначения редактируемой области
- `tape_loader.slpluginjs` — JS-плагин (экспорт): экспортирует текущий SCR-экран с перестановкой байтов для визуальных эффектов загрузки с ленты. Диалог экспорта с радиокнопками для выбора схемы загрузки и формата скачивания (ZIP-архив с .scr + .asm + .inc или только .scr). Шесть схем: **Backward** переворачивает все 6912 байт, чтобы загрузчик записывал их от $5AFF вниз до $4000 — сначала появляются атрибуты (снизу вверх), затем bitmap заполняется снизу вверх. **Linear** переупорядочивает данные построчно (8 bitmap-линий + 32 байта атрибутов на каждую знакоместную строку) — картинка рисуется сверху вниз, цвета появляются после каждой строки. **Checkerboard 2×2** делит экран на сетку 2×2 из блоков 16×12 знакомест, загружает блоки в шахматном порядке — сначала чётные позиции, затем нечётные; каждый блок загружает bitmap, затем атрибуты. **Checkerboard 4×3** использует сетку 4×3 из блоков 8×8 знакомест для более мелкого шахматного эффекта. **Checkerboard 8×6** использует сетку 8×6 из блоков 4×4 знакомест — самый мелкий шахматный паттерн. **Turbo Linear (2× speed)** экспортирует `.tzx` файл вместо `.tap` — TZX содержит стандартный BASIC-блок (Block $10) с турбо-загрузчиком в машинных кодах, встроенным в строку REM, и турбо-блок данных (Block $11) на удвоенной скорости (уменьшенные вдвое длительности импульсов). Турбо-загрузчик копирует себя в $8000 (неконтентная RAM) и читает данные через прямое определение фронтов на порту $FE с пороговым различителем бит (~52 такта/итерация). Возврат в BASIC через RST $08 (сообщение «0 OK»). Основано на технике турбо-загрузки из zxctl (iratahack). ZIP-экспорт для турбо-схемы включает .tzx + справочные .asm + .inc файлы. Первые пять схем используют ROM-процедуры ($0562 для поиска пилот-тона/синхро/флага, $05C6 для чтения байтов через приём с DE=0) и сохраняют/восстанавливают все регистры Z80 для корректного возврата в BASIC. Инклуд-файлы загрузчиков предоставляются отдельно для использования в других проектах. **Эмулятор дисковой загрузки**: для пяти нетурбо-схем при выборе формата ZIP флажок «Include disk emulator ASM» добавляет в архив автономный дисковый эмулятор — файл-обёртку `_disk.asm` и файл `disk_*_loader.inc` с Z80-ассемблером, который воспроизводит тот же визуальный эффект загрузки, но читает данные из буфера в RAM (`INCBIN`) вместо ленты. Используется тот же переупорядоченный `.scr` файл, что и для ленточного загрузчика (концепция monoloader — один `.scr` для обоих ASM). Скорость соответствует реальной ленте: 4 байта записываются в видеопамять за кадр через `HALT` (1728 HALT = ~34.6 секунд при 50fps). Нажатие любой клавиши во время загрузки показывает оставшуюся часть экрана мгновенно — инструкция `HALT` в подпрограмме `check_key` патчится в `NOP` через самомодифицирующийся код (SMC). Собирается sjasmplus с ORG $8000. Недоступно для турбо-схемы и форматов, отличных от ZIP
- `sca_diff_analyzer.slpluginjs` — JS-плагин (экспорт): открывает анимации SCA Type 0 и экспортирует покадровые различия на уровне знакомест в ZIP-архив с `.asm` исходниками для sjasmplus и бинарными `.bin` файлами. Кадр 0 всегда экспортируется как полный 6912-байтный `.bin`. Последующие кадры сравниваются по знакоместам с предыдущим кадром: если количество изменённых знакомест не превышает заданный порог, кадр кодируется директивами `DB` с координатами (строка, столбец), байтами bitmap и байтом атрибута для каждого изменённого знакоместа; иначе полный кадр экспортируется как `.bin` с ссылкой `INCBIN`. Диалог экспорта предоставляет: **Diff unit** — переключатель режима (Bitmap + атрибут: 9 байт/знакоместо, 8 байт bitmap в ZX-чередованном порядке + 1 байт атрибута; или Только атрибут: 1 байт/знакоместо); **Threshold** — числовое поле (1–768, максимум изменённых знакомест для использования diff вместо полного бинарника); **живая статистика** — количество diff- и full-кадров, оценка общего размера и цветная гистограмма изменённых знакомест по кадрам с жёлтой линией порога. ASM-файл включает метки `Frame_NNN:`, таблицу `DelayTable:` с задержками для каждого кадра (единицы 1/50 с) и покадровую сводку с размерами в байтах. Структура ZIP: `animation_diff/animation.asm` + `animation_diff/frame_NNN.bin` для полных кадров
- `cursor_loader.slpluginjs` — JS-плагин (экспорт): экспортирует текущий SCR-экран с интерактивным порядком загрузки по знакоместам, эффектом бегущего курсора и поддержкой **нескольких источников**. Диалог экспорта отображает экран в масштабе 2× (512×384) с наложенной сеткой 32×24 знакоместа. **Несколько источников**: экран редактора — источник 1; нажмите **+ Add** для загрузки дополнительных `.scr`-файлов как пронумерованных источников. Цветная панель вкладок показывает все источники — щёлкните для переключения, × для удаления. Ячейки активного источника отображаются с полной яркостью и цветной рамкой; ячейки других источников — с тонированным наложением цвета источника. Неназначенные ячейки показывают затемнённую чёрно-белую версию исходного изображения с наложением диагонального креста, сохраняя видимость структуры картинки при расстановке ячеек. **Взаимодействие**: левый щелчок или перетаскивание добавляет ячейки от активного источника (перетаскивание интерполирует по Брезенхему для плавных линий); правый щелчок устанавливает якорь (показан белыми угловыми скобками), повторный правый щелчок рисует линию Брезенхема от якоря до цели. Одна и та же позиция ячейки может появляться несколько раз от разных источников, создавая эффект замены одной картинки другой. Пять пресетов заполняют все 768 позиций от активного источника: **Typewriter** (слева направо, сверху вниз), **Columns** (сверху вниз, слева направо), **Zigzag** (чередование направления в каждой строке), **Spiral In** (спираль по часовой стрелке от краёв к центру), **Random** (перемешивание Фишера-Йейтса). Инструменты: **Clear** сбрасывает весь порядок, **Fill remaining** заполняет непокрытые ячейки от активного источника, **Undo** / **Redo** (Ctrl+Z / Ctrl+Y), **Save** / **Load** (состояние проекта в/из `.json`-файла, включая все источники в base64), **Animate** — флажок для предпросмотра анимации загрузки в любой момент, даже при частичном покрытии. **Initial colors**: выпадающие списки Ink, Paper и флажок Bright задают начальное заполнение атрибутов и цвет бордюра перед загрузкой. Цветовой образец показывает глиф «A» из ROM ZX в цветах ink на paper. **Cursor toggle**: флажок «Show cursor during loading» управляет отображением яркой белой ячейки-курсора ($78) перед данными. **Border stripes**: выпадающий список выбирает цветовую схему полос бордюра — Blue/Yellow (стандартная), Red/Cyan, Magenta/Green, Black/White или Solid (без полос); режим Solid копирует ROM-процедуру чтения байтов в неконтентную RAM через LDIR, патчит абсолютные адреса для релокации и заменяет OUT ($FE),A на NOP NOP; появляется выпадающий список **Color** для выбора любого из 8 цветов ZX Spectrum в качестве цвета сплошного бордюра. Экспорт доступен после покрытия всех 768 уникальных позиций. Статус: «Entries: N | Unique cells: M / 768». Каждая ячейка = 9 байт (8 bitmap-линий + 1 байт атрибута). Машинный код Z80 (~110 байт) и таблица порядка ячеек (N × 2 байта) встраиваются в строку REM по адресу $5CD0. Для каждой ячейки загрузчик: (1) записывает $78 (яркая белая бумага) в адрес атрибута как видимый курсор, (2) читает 8 bitmap-байтов с ленты и записывает их по правильным нелинейным адресам экрана, (3) читает 1 байт атрибута и перезаписывает курсор реальным цветом. Использует ROM-процедуры ($0562 для пилот-тона/синхро/флага, $05C6 для чтения байтов через приём с DE=0). Выход: .tap-файл с BASIC-программой (автозапуск строки 10 с `RANDOMIZE USR 23760`) + безголовый блок данных (флаг $FE, N×9 переупорядоченных байтов). Формат скачивания: только TAP или ZIP-архив с .tap + переупорядоченный `.bin` + справочный .asm (директивы SAVETAP для sjasmplus) + .inc (комментированная Z80-ассемблерная программа с таблицей ячеек в виде DB-строк). **Эмулятор дисковой загрузки**: при выборе формата ZIP флажок «Include disk emulator ASM» добавляет в архив автономный дисковый эмулятор — файл-обёртку `_disk.asm` и файл `disk_cursor_loader.inc` с Z80-ассемблером, который воспроизводит тот же визуальный эффект поячеечной загрузки с курсором, но читает данные из буфера в RAM (`INCBIN`) вместо ленты. Используется тот же компактный поток `.bin`, что и для ленточного загрузчика (концепция monoloader — один `.bin` для обоих ASM). Скорость соответствует реальной ленте: 3 HALT на ячейку (~60мс ≈ ~62мс ленты); bitmap-байты появляются поступенно в монохроме (атрибут курсора) за 3 кадра, затем байт атрибута применяет цвет. Нажатие любой клавиши во время загрузки показывает оставшийся экран мгновенно (HALT патчится в NOP через SMC). Собирается sjasmplus с ORG $8000. Флажок скрыт при выборе формата TAP

---

## 28. Script Editor — Скриптовый движок

**Script Editor** — масштабируемая перетаскиваемая плавающая панель со встроенным скриптовым языком в стиле BASIC для автоматизации рисования, пакетной обработки и генеративного искусства. Открывается через вкладку **Tools** → ссылку **Script Editor**. Перетаскивание за заголовок — перемещение, за правый нижний угол — изменение размера. Положение и размер панели сохраняются между сессиями.

### Редактор скриптов

Моноширинный текстовый редактор с нумерацией строк для ввода скриптов. Длинные строки прокручиваются горизонтально. Содержимое автоматически сохраняется в localStorage между сессиями.

**Кнопки панели инструментов:**

| Кнопка | Действие |
|--------|----------|
| **▶ Run** | Выполнить скрипт (также **Ctrl+Enter**) |
| **■ Stop** | Прервать выполняемый скрипт |
| **Clear Log** | Очистить лог вывода |
| **Load** | Загрузить файл `.slscript` / `.txt` / `.bas` |
| **Save** | Скачать скрипт как файл `.slscript` |
| **?** | Открыть/закрыть встроенный справочник языка |

**Выпадающий список Examples** — загружает один из 7 встроенных демо-скриптов (Diagonal Line, Starfield, Gradient Bars, Checkerboard, Sine Wave, Circles, Sierpinski Triangle).

### Лог вывода

Отображает вывод `PRINT` и статусные сообщения. Ошибки выделяются красным с указанием номера строки.

### Справочник языка

**Переменные и присваивание:**

```
LET x = 10
y = x + 5
```

Переменные по умолчанию равны `0`, если ранее не были присвоены. Ключевые слова регистронезависимы — `setink`, `SETINK`, `SetInk` работают одинаково.

**Управление потоком:**

```
FOR i = 0 TO 255 STEP 2
  PIXEL i i
NEXT

IF x > 128 THEN
  SETINK 7
ELSE
  SETINK 0
ENDIF

REPEAT 100
  PIXEL RANDOM(256) RANDOM(192)
ENDREPEAT

WHILE x < 256
  PIXEL x 96
  x = x + 1
ENDWHILE
```

**Пользовательские функции:**

```
FUNC drawStar(cx, cy, size)
  LINE cx - size cy cx + size cy
  LINE cx cy - size cx cy + size
ENDFUNC

CALL drawStar(128, 96, 20)
```

**Комментарии:** `#` или `REM` — всё после них игнорируется до конца строки.

### Команды рисования

| Команда | Параметры | Описание |
|---------|-----------|----------|
| `PIXEL` | x y | Установить пиксель текущим цветом ink |
| `PIXELPAPER` | x y | Установить пиксель текущим цветом paper |
| `PLOT` | x y | Псевдоним для `PIXEL` |
| `LINE` | x0 y0 x1 y1 | Нарисовать линию |
| `RECT` | x0 y0 x1 y1 | Нарисовать контур прямоугольника |
| `FILLRECT` | x0 y0 w h | Нарисовать заполненный прямоугольник |
| `CIRCLE` | cx cy rx ry | Нарисовать эллипс (ry по умолчанию = rx) |
| `FILL` | x y | Заливка из точки |
| `CLEAR` | [ink paper] | Очистить экран (опционально задать цвета) |

### Команды атрибутов

| Команда | Параметры | Описание |
|---------|-----------|----------|
| `SETINK` | n | Установить текущий цвет ink (0–7) |
| `SETPAPER` | n | Установить текущий цвет paper (0–7) |
| `SETBRIGHT` | b | Установить флаг яркости (0 или 1) |
| `SETFLASH` | b | Установить флаг мерцания (0 или 1) |
| `SETATTR` | col row ink paper bright flash | Установить атрибут ячейки (col, row) |

### Операции с экраном

| Команда | Описание |
|---------|----------|
| `RENDER` | Принудительная перерисовка экрана (обычно автоматически в конце) |
| `UNDO` | Отменить последнее изменение |
| `REDO` | Повторить отменённое изменение |

### Функции запроса

| Функция | Возвращает |
|---------|------------|
| `GETPIXEL(x, y)` | 1, если пиксель установлен, иначе 0 |
| `GETINK(col, row)` | Цвет ink в ячейке атрибутов |
| `GETPAPER(col, row)` | Цвет paper в ячейке атрибутов |
| `GETBRIGHT(col, row)` | 1, если яркость включена, иначе 0 |
| `WIDTH()` | Ширина текущего формата в пикселях |
| `HEIGHT()` | Высота текущего формата в пикселях |

### Математические функции

`SIN(x)`, `COS(x)`, `TAN(x)`, `SQRT(x)`, `ABS(x)`, `FLOOR(x)`, `CEIL(x)`, `ROUND(x)`, `MIN(a,b)`, `MAX(a,b)`, `RANDOM(n)` (от 0 до n−1), `PI`.

### Операторы

Арифметические: `+`, `-`, `*`, `/`, `%`. Сравнения: `=`, `<>`, `!=`, `<`, `>`, `<=`, `>=`. Логические: `AND`, `OR`, `NOT`.

### Особенности выполнения

- Скрипты выполняются асинхронно, уступая управление каждые 1000 инструкций для отзывчивости браузера
- Перед запуском скрипта сохраняется одна точка отмены — весь скрипт является одним шагом Undo
- Экран перерисовывается один раз по завершении скрипта (используйте `RENDER` для промежуточных перерисовок)
- Размер кисти временно устанавливается в 1 пиксель на время выполнения скрипта
- Кнопка Stop прерывает выполнение в ближайшей точке уступки

---

## 29. Справочник поддерживаемых форматов

### Редактируемые форматы

| Расширение | Размер | Описание |
|------------|--------|----------|
| .scr | 6912 байт | Стандартный экран (битмап + атрибуты) |
| .rcs | 6912 байт | [RCS](https://github.com/einar-saukas/RCS) переупорядоченный экран (автоконвертируется в SCR при загрузке) |
| .scr.zx7 | переменный | [ZX7](https://spectrumcomputing.co.uk/entry/27996/ZX-Spectrum/ZX7) сжатый экран (автодекомпрессия при загрузке) |
| .scr.zx7b | переменный | ZX7 обратное сжатие экрана (автодекомпрессия при загрузке) |
| .rcs.zx7 | переменный | RCS переупорядоченный + ZX7 сжатый (автодекомпрессия и обратное упорядочивание при загрузке) |
| .rcs.zx7b | переменный | RCS переупорядоченный + ZX7 обратное сжатие (автодекомпрессия и обратное упорядочивание при загрузке) |
| .scr.zx0 | переменный | [ZX0](https://github.com/einar-saukas/ZX0) сжатый экран (автодекомпрессия при загрузке) |
| .scr.zx0b | переменный | ZX0 обратное сжатие экрана (автодекомпрессия при загрузке) |
| .rcs.zx0 | переменный | RCS переупорядоченный + ZX0 сжатый (автодекомпрессия и обратное упорядочивание при загрузке) |
| .rcs.zx0b | переменный | RCS переупорядоченный + ZX0 обратное сжатие (автодекомпрессия и обратное упорядочивание при загрузке) |
| .scr.lc | переменный | Laser Compact 5.2.1 сжатый экран с заголовком LCMP5 (автодекомпрессия при загрузке) |
| .scr.c4 | 841 байт | Chunks 4×4 монохромный битмап с потерями — словарь из 4 паттернов, 1 байт/ячейку (768Б + 64Б LUT + 8Б словарь + 1Б режим) |
| .scr.c2 | 1573 байта | Chunks 4×2 монохромный битмап с потерями — словарь из 4 паттернов, 2 байта/ячейку (1536Б + 32Б LUT + 4Б словарь + 1Б режим) |
| .scr.lzf | переменный | [ZXSC](https://github.com/TomDDG/ZXSC---ZX-Spectrum-Screen-Compresser) LZF сжатый экран — стандартный или screen-scan режим (автодекомпрессия при загрузке) |
| .scr.rle | переменный | RLE (PackBits-стиль) сжатый экран (автодекомпрессия при загрузке) |
| .scr.upk | переменный | upkr сжатый экран с настройками Z80 (автодекомпрессия при загрузке) |
| .scr.lgk | переменный | LgK v1.1rs сжатый экран (автодекомпрессия при загрузке) |
| .scr | 6976 байт | ULA+ (палитра 64 цвета) |
| .scr | 6945–7426 байт | ULANext (расширенная палитра Next, до 256 цветов) |
| .scr | 6144 байт | Монохромный полный |
| .scr | 4096 байт | Монохромный 2/3 |
| .scr | 2048 байт | Монохромный 1/3 |
| .53c / .atr | 768 байт | Только атрибуты |
| .bsc | 11136 байт | Экран с бордюром |
| .bsp | переменный | Экран с бордюром и заголовком (70-байтный заголовок, экран/gigascreen +/- бордюр) |
| .ifl | 9216 байт | Мультиколор 8×2 |
| .mlt / .mc | 12288 байт | Мультиколор 8×1 |
| .bmc4 | 11904 байт | Мультиколор 8×4 + бордюр |
| .3 | 18432 байт | Триколор RGB |
| .img | 13824 байт | Gigascreen (2×SCR) |
| .ga | 7680 байт | Gigaattr (общий битмап + 2×атрибуты) |
| .gap | 7744 / 7808 байт | Gigaattr+ULA+ (общая палитра / двойная палитра) |
| .hlr | 1536 байт | High Lores (32×24 крупных пикселя, gigascreen) |
| .stl | 3072 байт | Stellar (64×48 крупных пикселей, мультиколорный gigascreen) |
| .sca | переменный | Анимация (полные/атрибутные кадры) |
| .specscii | переменный | Текстовый режим (32×24 символа + OVER-слои) |
| .ch$ / .chr$ / .ch- | переменный | Массив символов (чередующиеся ячейки 8×8) |
| .zxp | переменный | ZX-Paintbrush (переменный размер, палитра ULA или ULA+) |
| .btile | переменный | Nirvana btile (переменный размер, мультиколор 8×2) |
| .wtile | переменный | Nirvana wtile (переменный размер, мультиколор 8×2) |
| .nxi | 49664 байт | ZX Next Layer 2 256×192 + встроенная палитра RGB333 (256-цветный индексированный) |
| .nxi | 82432 байт | ZX Next Layer 2 320×256 + встроенная палитра RGB333 (256-цветный, column-major) |
| .nxi | 81952 байт | ZX Next Layer 2 640×256 + встроенная палитра RGB333 (16-цветный, 4bpp column-major) |
| .sl2 | 49152/49280 байт | ZX Next Layer 2 256×192, палитра RGB332 по умолчанию (256-цветный индексированный) |
| .sl2 | 49664 байт | ZX Next Layer 2 256×192 + встроенная палитра RGB333 |
| .sl2 | 81920 байт | ZX Next Layer 2 320×256 или 640×256 (диалог выбора режима) |
| .sl2 | 82432 байт | ZX Next Layer 2 320×256 + встроенная палитра RGB333 |
| .sl2 | 81952 байт | ZX Next Layer 2 640×256 + встроенная палитра RGB333 (16-цветный, 4bpp) |
| .c | 32768 байт | Scorpion GMX 640×200 hi-res (битмап + атрибуты 8×1, строки удвоены для отображения) |
| .c | 16128 байт | Scorpion GMX 160×200 только атрибуты (заголовок "GMX\x0F", атрибуты 8×1, пиксели удвоены по горизонтали + строки удвоены для отображения = 640×400) |

### Форматы только для просмотра

| Расширение | Размер | Описание |
|------------|--------|----------|
| .zip | переменный | Архив (автоизвлечение, выбор файла из списка) |

### Импорт (конвертация в ZX Spectrum)

`.png`, `.gif`, `.jpg`, `.jpeg`, `.webp`, `.bmp` — через диалог Image Import с дизерингом и настройками. Анимированные GIF (многокадровые) импортируются напрямую как SCA-анимации с сохранением задержек кадров.

### Форматы тайлов Nirvana (импорт в IFL + набор спрайтов)

| Расширение | Описание |
|------------|----------|
| .btile | Nirvana btile (2×2 знакоместа, 16×16px, 48 байт/тайл) |
| .wtile | Nirvana wtile (3×2 знакоместа, 24×16px, 72 байт/тайл) |

### Форматы снапшотов (для Memory Viewer)

| Расширение | Описание |
|------------|----------|
| .sna | Снапшот SNA |
| .z80 | Снапшот Z80 |

### Форматы проектов

| Расширение | Описание |
|------------|----------|
| .slp | Проект со слоями |
| .slw | Рабочее пространство (несколько картинок) |
| .slb | Набор пользовательских кистей |
| .slbc | Набор штрихкодов |
| .sls | Лист спрайтов |

---

## ZGS Editor

ZGS Editor — автономный инструмент (`zgs_editor.html`) для создания и редактирования векторных сцен ZGS (ZX Graphics Script). Открывается по ссылке **ZGS Editor** на вкладке Tools основного приложения.

### Компоновка

- **Левая панель** — текстовый редактор исходного кода `.zgt`
- **Правая панель** — холст предпросмотра, панели инструментов рисования и команд, элементы воспроизведения

### Файловые операции

| Кнопка | Действие |
|--------|----------|
| **Open** | Открыть `.zgs` (бинарный, авто-дизассемблируется), `.zgt` (текст) или `.zgp` (проект) |
| **New** | Добавить новую пустую сцену (или сбросить проект, если все сцены пустые) |
| **Save** | Выпадающее меню: Save .zgs (бинарный), .zgt (текст), .zgp (проект), .asm (Z80 ассемблер ZIP) |

Файлы также можно перетаскивать на страницу.

### Управление воспроизведением

| Элемент | Действие |
|---------|----------|
| **Render** | Ассемблировать и отрисовать сцену мгновенно |
| **Play** / **Pause** | Пошаговое анимированное воспроизведение |
| **Step** | Выполнить один опкод и перерисовать; подсвечивает соответствующую строку в исходнике |
| **Speed** | Задержка между шагами при Play (5–500 мс, по умолчанию 37 мс) |
| **Zoom** | Масштаб холста: x1, x2, x3, x4, x5 |
| **Pen** | Переключить отображение перекрестия пера |
| **Grid** | Переключить сетку знакомест 8×8 (оранжевые линии, яркие разделители третей на y=64/128). Сохраняется в localStorage |

### Референсное изображение (сворачиваемый блок)

Загрузка внешнего изображения в качестве образца для рисования, отображаемого поверх холста предпросмотра. Каждая сцена хранит своё референсное изображение и настройки. Заголовок секции переключает открытие/закрытие (состояние сохраняется в localStorage).

- **Load** — выбрать файл изображения (любой формат, поддерживаемый браузером)
- **Clear** — удалить референсное изображение из активной сцены
- **Show** — переключить видимость без удаления изображения
- **Opacity** — ползунок прозрачности (5%–80%, по умолчанию 30%)
- **X / Y** — смещение позиции в пикселях (по умолчанию 0, 0)
- **W / H** — размер отображения в пикселях ("auto" = 256×192, соответствует экрану ZGS)

При добавлении новой сцены референсное изображение и настройки текущей сцены копируются в новую. Референсные изображения сохраняются в файлах проекта `.zgp` (формат v2), с дедупликацией между сценами для экономии размера. Файлы `.zgp` версии 1 загружаются нормально (сцены без референса).

### Переключение темы

Нажмите кнопку **☾/☀** в заголовке для переключения между тёмной и светлой темами. Настройка общая с основным SpectraLab и Font Editor через один ключ `spectraLabTheme` в localStorage.

### Инструменты рисования (панель фигур)

Выберите инструмент, затем кликните или перетащите курсор по холсту. Сгенерированная инструкция вставляется перед `end`, и холст перерисовывается мгновенно.

| Инструмент | Действие | Генерируемая инструкция |
|------------|----------|-------------------------|
| **Cursor** | ЛКМ копирует `lx, ly` в буфер обмена | — |
| **Dot** | Клик — поставить точку | `dot_abs lx, ly` |
| **Line** | ЛКМ-перетаскивание: одна линия. ПКМ-перетаскивание: полилиния (резинка следует от последней точки; ПКМ для новых сегментов; ЛКМ или Esc для завершения) | `move_abs x0, y0` + `line_dmed dx, dy` |
| **Rect** | Перетащить для задания прямоугольника | `rect_outline_abs x, y, w, h` |
| **RectF** | Перетащить для заливки прямоугольника | `rect_fill_abs x, y, w, h` |
| **Circle** | Перетащить от центра к радиусу | `circle_outline_abs cx, cy, r` |
| **CircleF** | Перетащить от центра к радиусу | `circle_fill_abs cx, cy, r` |
| **Ellip** | Перетащить от центра к краю для задания радиусов | `ellipse_outline_abs cx, cy, rx, ry` |
| **EllipF** | Перетащить от центра к краю для задания радиусов | `ellipse_fill_abs cx, cy, rx, ry` |
| **Flood** | Клик для заливки | `flood_abs lx, ly` |
| **Text** | Клик для установки позиции курсора | `set_cursor col, row` |
| **ClearR** | Перетащить для выбора знакомест | `clear_region col, row, w, h, attr` |

При перетаскивании жёлтый оверлей показывает предпросмотр фигуры. ClearR привязывается к сетке знакомест 8×8 и показывает красную пунктирную рамку.

**Клавиши-модификаторы фигур:** Удерживайте клавиши-модификаторы при перетаскивании Rect, RectF, Circle, CircleF, Ellip или EllipF:

| Модификатор | Эффект |
|-------------|--------|
| **Ctrl** | Ограничить соотношение 1:1 (квадрат / круг) |
| **Alt** | Рисовать от центра вместо угла |
| **Ctrl+Alt** | Оба вместе |

### Панель текста

При выборе инструмента **Text** появляется панель с элементами:

- **Информация о курсоре** — показывает текущую позицию курсора после клика по холсту
- **Режим текста** — выпадающий список для выбора ширины символа: **32 col** (8 пикселей, стандарт), **42 col** (6 пикселей), **64 col** (4 пикселя). Каждый режим использует независимое отслеживание курсора и адрес шрифта
- **Поле ввода текста** — введите текст для печати
- **Кнопка Print** — вставляет `print_text "..."`, `print_text_42 "..."` или `print_text_64 "..."` в зависимости от выбранного режима текста

Сначала кликните по холсту для установки команды `set_cursor` (вариант зависит от режима: `set_cursor col, row`, `set_cursor_42 col, row` или `set_cursor_64 col, row`), затем введите текст и нажмите Print для вставки соответствующей команды печати. Две операции разделены, что позволяет рисовать между размещением курсора и вводом текста.

Текст рендерится шрифтом ROM ZX Spectrum (8×8 для 32-колоночного, 6×8 для 42-колоночного, 4×8 для 64-колоночного, символы 32–127) с текущим атрибутом. Курсор продвигается после каждого символа и переносится на следующую строку по достижении предела колонок (32, 42 или 64). Шрифт 6×8 использует столбцы 0-5 из 8×8 ROM-шрифта. Шрифт 4×8 получен операцией OR над парами столбцов из 8×8 шрифта (выходной_бит[n] = входной_бит[2n] | входной_бит[2n+1]).

#### Упакованный текст (`print_packed`)

`print_packed "строка"` работает визуально идентично `print_text`, но использует словарное сжатие для уменьшения размера байткода на 30–50% для английского текста. Кодер использует словарь распространённых биграмм, триграмм и слов. Используйте для длинных текстовых пассажей (описания в adventure-играх, диалоги). Директива `.dict` выбирает словарь кодирования:

- `.dict lower` — встроенный словарь английского строчного (по умолчанию)
- `.dict upper` — встроенный словарь английского заглавного
- `.dict user` — пользовательский словарь, загруженный из файла `.zdict`

Для использования пользовательского словаря выберите «Dict: user» в выпадающем списке на панели текста и нажмите «Load .zdict» для загрузки файла словаря. Пользовательские словари можно генерировать утилитой `zgs_mkdict.py` из текстовых файлов игры. Словарь блокируется после вставки первой команды `print_packed`, чтобы избежать рассогласования кодирования.

### Панель команд

Вставка команд перед `end` через выпадающие списки и кнопки:

| Элемент | Вставляет |
|---------|-----------|
| **Ink...** | `set_ink <цвет>` (+ `bright` если Brt включён). Метка показывает выбранное значение (напр., «Ink: red») |
| **Paper...** | `set_paper <цвет>` (+ `bright` если Brt включён). Метка показывает выбранное значение |
| **Brt** | Добавляет `bright` к следующей команде Ink/Paper |
| **Pattern...** | `set_pattern <имя>`. Метка показывает выбранное значение (напр., «Pat: checker») |
| **XOR** | `set_mode xor` |
| **SET** | `set_mode set` |
| **Clear** | `clear_region 0, 0, 32, 24, <attr>` (весь экран, текущий атрибут) |
| **WaitKey** | `wait_key` — пропускается при мгновенной отрисовке; при анимированном воспроизведении ожидает нажатия клавиши или клика по холсту |
| **End** | `end` |

### Отображение координат

Наведите курсор на холст:
- **Подсказка** (правый верхний угол холста) — логические координаты `lx, ly`
- **Строка состояния** — логические и пиксельные координаты `x: lx  y: ly  (px: px, py)`

ПКМ в любом месте холста — скопировать координаты в буфер обмена.

### Перекрестие пера

При включённом флажке **Pen** полупрозрачное зелёное перекрестие отмечает позицию пера VM. Обновляется после Render, Play и Step.

### Синхронизация строк исходника

При использовании **Step** в текстовом редакторе подсвечивается строка, соответствующая текущему счётчику программы VM. Работает на основе карты исходного кода, формируемой при ассемблировании.

### Текстовый редактор

- Авто-рендер с задержкой 500 мс при вводе
- Клавиша **Tab** вставляет два пробела
- Комментарии начинаются с `;`
- Строки после `end` игнорируются ассемблером

### Горячие клавиши

| Сочетание | Действие |
|-----------|----------|
| Ctrl+Z | Отмена |
| Ctrl+Y / Ctrl+Shift+Z | Повтор |
| Tab | Вставить два пробела |
| Escape | Отменить активную полилинию |
| Alt+Стрелки | Сдвиг выделенных строк — смещение абсолютных координат на ±1 (сначала выделите строки) |
| Ctrl (при перетаскивании) | Ограничить фигуру соотношением 1:1 (квадрат/круг) |
| Alt (при перетаскивании) | Рисовать фигуру от центра вместо угла |

> **Примечание:** Горячие клавиши работают с любой раскладкой клавиатуры (русская, немецкая и т.д.) — они привязаны к физическому положению клавиш.

---

## Благодарности и сторонние библиотеки

- **ZX7** — сжатие, автор [Einar Saukas](https://spectrumcomputing.co.uk/entry/27996/ZX-Spectrum/ZX7)
- **ZX0** — сжатие, автор [Einar Saukas](https://github.com/einar-saukas/ZX0)
- **RCS** — переупорядочивание экрана, автор [Einar Saukas](https://github.com/einar-saukas/RCS)
- **Laser Compact 5.2.1** — сжатие, автор Hrumer (пакер/депакер, 1994–2014), Eugene Larchenko (исправление переполнения буфера, поддержка сегментов)
- **upkr** — сжатие, автор phar/Loonies ([GitHub](https://github.com/exoticorn/upkr))
- **ZXSC** — LZF-компрессор экрана с нелинейным посимвольным сканированием, автор [TomDDG](https://github.com/TomDDG/ZXSC---ZX-Spectrum-Screen-Compresser) (лицензия MIT). Z80-распаковщики и дизайн алгоритма из оригинального проекта; компрессор переимплементирован на JavaScript с оптимальным DP-разбором
- **LgK** — LgK v1.1rs (Lethargeek Kompakt, Row-Sequence edition) тайловый компрессор экрана, автор Lethargeek. Порт на JavaScript — Bedazzle
